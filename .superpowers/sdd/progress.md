EDPP coeff-wiring plan execution. Plan: docs/superpowers/plans/2026-06-24-edpp-rule-coeffs-wiring.md
Task 1: complete (commits b99e280..449b8c9, review clean)
  Minor (for final review): validate() branches AlphaP<=0, C1<0, CPf<=0 lack dedicated test cases (spec fixture under-covered).
  Minor: alpha-divergence denominator is AlphaD (spec unspecified; well-defined).
Task 2: complete (commits 449b8c9..feb3404, review clean)
  Minor (for final review): muPrefill clamp not independently exercised (symmetric with muDecode via clampMu).
Task 3: complete (commits feb3404..d29b93f, review clean — accessor reads RunningBatch.Requests, same as BatchSize; both refresh paths)
Task 4: complete (commits d29b93f..090e45c, review clean — 23/23 TestEDPP green)
  Minor (for final review): tauGuard test comment says "< AlphaD" but guard is "<=" (comment precision).
Task 5: complete (commits 090e45c..20722b7, review clean + 1 fix)
  Fix 20722b7: made TestEDPP_AlphaZero probe-free (forward-compat with Task 8 probe deletion).
  Note: probe-only tests TestEDPP_ExtractAlpha_CancelsDelta / TestEDPP_MarginalDelta_RecoversPerCopyWork still reference probe helpers — Task 8 deletes them.
Task 6: complete (commits 20722b7..717123c, incl 1 critical fix, re-reviewed clean)
  Critical FIXED (717123c): disaggregated-completion key mismatch leaked qp/qd/pending; now correlated to parent ID via edppConservationKey. Conservation test added.
  Important FIXED: Forget() cleanup wired to all terminal paths (timeout/drop/empty-signal).
  CARRY-FORWARD for Task 9 (finding D): implementer added DeploymentConfig.EDPPCoeffs (sim.EDPPCoeffs) field + cluster.go sets EDPPConfig.Coeffs=config.EDPPCoeffs. Task 9's --edpp-coeffs MUST load INTO this existing field (single mechanism), NOT add a competing EDPPCoeffsPath path-load that bypasses it. Reconcile.
  Note (for final review): EDPP does not naturally disaggregate in cluster test harness (decode snapshot Qd=0 at decision, ITL never breaches) — orthogonal follow-up; Task 7 swaps Decide to bookkept qpWork/qdWork which may affect this.
  Minor: edppConservationKey fallback parentRequests scan is O(n) (only on non-normal path).
Task 7: complete (commit 21e0e70, review clean — all predictor/test arithmetic reconciled)
  Minor: selectedDecodeITL now uncalled (Task 8 deletes); ITLP/ITLD trace field comments stale (always 0 now).
Task 8: complete (commits 21e0e70..3fcbb11, review clean + 1 coverage fix)
  Fix 3fcbb11: restored chunk-cap coverage (TestEDPP_ChunkCap, capped 2560 vs uncapped 10000).
  Extra deletion: TestEDPP_DeltaPfChunk (called deleted chunkInflation).
Task 9: complete (commits 3fcbb11..4a2be34, review clean — reconciled design: --edpp-coeffs loads into existing DeploymentConfig.EDPPCoeffs; both run+replay sites wired)
  Minor (FIX IN FINAL WAVE): TestResolveEDPPCoeffs_FrozenLlama70b tolerance is 1.0 — tighten to 1e-6 using exact 16613.539607002218 (cmd/root_test.go or wherever the test lives).
Task 10: complete (commits 4a2be34..2368a1c, review clean + 1 strengthening fix)
  Fix 2368a1c: dimensionless anchor now asserts LHS/RHS exact invariance across k + boundary-true case + TTFT term. All 3 §11 anchors pass.
Task 11: complete (commit 3296ea3, review clean — docs accurate, no deleted-symbol refs). NOTE: docs/superpowers/ is gitignored; design+plan artifacts remain on-disk only.
ALL 11 TASKS COMPLETE. Final whole-branch review next.

FINAL WHOLE-BRANCH REVIEW (opus): READY TO MERGE. No Critical/Important blockers.
  - Conservation sound across all terminal paths; INV-9/INV-6/R3/R4 intact; §11 anchors genuine.
  - Minors a,b,c,e fixed in 14d2cba; d (O(n) fallback) left as acceptable.
  IMPORTANT FOLLOW-UP (tracked, non-blocking): latent under-disaggregation bias — (1) deferred μ·Δt drain + P-work held to decode-completion overstates qpWork (damps further disagg); (2) decode-side TTFT measurement understates z_ttft for P-routed reqs. Recommend follow-up: implement μ·Δt drain or release P-work at prefill-complete; reconsider decode-side TTFT.
  Minor: OnRoute ap (raw len(InputTokens)) vs Decide ap (cache-adjusted) asymmetry — intentional/benign, undocumented at call site cluster.go:2030.

=== PLAN 2 (2026-06-25): waiting-only backlog + co-residency predictors ===
Base (PLAN 1 final) = 14d2cba. Plan: docs/superpowers/plans/2026-06-25-edpp-waiting-only-backlog.md
P2-Task 1: complete (commit 3acec64, review clean — TTFT_P uses T_pf(B−1); S_pf=0 tests unaffected)
P2-Task 2: complete (commits 3acec64..d395710, review clean + minors fix d395710 — OnAdmit kernel hook, tick+nil tests, preemption caveat doc)
P2-Task 3: complete (commits d395710..9eb5af7, opus review + 1 important fix)
  Waiting-only migration: OnAdmit drains at admission, OnComplete stops draining; cluster feedAdmission + 2-site wiring; correlation keys MATCH all 3 cases (D / prefill+"_prefill" / decode+"_decode").
  Important FIXED (9eb5af7): added TestEDPP_Cluster_ConservationViaAdmission_NormalCompletion — covers OnAdmit drain for normally-completing (OnComplete-path) requests; revert-verified (leaks qd=3458,pending=1 w/o wiring). Prior conservation tests only exercised Forget-path (forced-disagg artifact).
P2-Task 4: complete (commit 801766b, docs-only — backlog comment now waiting-only; no stale 'over-hold'/'removed at completion' refs)
P2-Task 4: complete (commit 801766b, docs-only).
P2 FINAL WHOLE-BRANCH REVIEW (opus): READY TO MERGE. Conservation sound end-to-end (3 cases); test gap closed (unit + revert-verified integration); predictors symmetric, no live-μ coupling; INV-9/INV-6/R4 intact.
  Minor FIXED (4b4504a): stale cluster.go:1220 OnComplete comment corrected.
PLAN 2 COMPLETE (14d2cba..4b4504a).

=== PLAN 3 (2026-06-25): EDPP empirical study ===
Base = 4b4504a. Plan: docs/superpowers/plans/2026-06-25-edpp-empirical-study.md
Task 1: complete (commit 4b4504a..5079659, review clean — Spec✅ quality Approved)
  Minor (final review): test asserts was_disaggregated key-presence but not prefill/decode_instance_id key-presence (field round-trip already covers them).
  ⚠️ resolved by controller: whole-tree build clean; only SimResult{} site is named-field literal at replay.go:874.
Task 2: complete (commit 5079659..826deec, review clean — Spec✅ quality Approved, no blockers)
  Adaptation: 8 existing test call sites updated to extractSimResults(m, nil).
  CARRY-FORWARD to Task 4: reviewer ⚠️ — verify cs.ParentRequests() is actually POPULATED during real replay (grep results JSON for was_disaggregated in Task 4 first run).
Task 3: complete (commit 826deec..4b602a5, review clean — Spec✅ quality Approved). Generated specs gitignored.

DESIGN DECISION (2026-06-25, user-confirmed) — topology + P/D-ratio axis:
- Code reality (verified): non-disagg/local path routes to DECODE pool; FormBatch prefills any ProgressIndex==0 req regardless of role, so a --decode-instances (decode-ONLY role) instance DOES run prefill locally for local requests. So "D = does both" maps to --decode-instances, NOT --prefill-decode-instances. Confirmed by TestDisaggregation_NonDisaggRoutedToDecodePoolOnly + newTestDisaggDeploymentConfig(4,2,2) (no shared pods).
- never/all-local baseline = --num-instances 4 homogeneous (each does both). NOT forced onto a split — equal-hardware comparison is INTENTIONAL, not a confound.
- Disaggregating arms (edpp/always/prefix-threshold) swept over P/D SPLITS at 4 total: 1P3D, 2P2D, 3P1D (the P/D-ratio study axis).
- Trace baked ONCE per (wl,rate) at num-instances 4 (topology-independent request stream), replayed across all decider×split combos.
- File naming: results_<wl>_rate<R>_<decider>_<tag>.json (tag = agg for never, else 1P3D/2P2D/3P1D); decisions_<wl>_rate<R>_edpp_<tag>.csv.
- Grid: 2 wl × 6 rates × {never@4 + (edpp/always/prefix)×3 splits} = 12 bakes + 120 replays.
- Task 4 (commit d61fd29) sweep.sh must be REWRITTEN for this; Task 6 glob/regex must parse decider+split.
Task 4: complete (REVISED for P/D-split design; new commit). Smoke synth/1.0: 10 results + 3 decisions, topology (1P3D/2P2D/3P1D) ACCEPTED (no validation error), disagg field populated end-to-end (always@2P2D=5000/5000; edpp=0 at low rate expected; never=0). RESOLVES Task2 carry-forward (ParentRequests populated in replay). num_requests=5000.
Task 5: complete (window.py — steady_window converges@204 on synth/1.0; offered_work rag=15255/500 synth=2500/4000 tok/s, correct P/D-boundedness).
Task 6: complete (report.py — tier1 summary, regret join 1141 misses joined to dominant term, 6 plots). Validated on smoke cell.
  Minor (final review): regret_join uses full (un-windowed) request set while summary uses steady-state slice — exploratory regret may include warmup. Acceptable; note in findings.
  Preview finding: synth/1.0 edpp(all-local) ttft_p99 125ms < always 142ms @1P3D — declines to disagg decode-bound load.
Task 7: complete (FINDINGS.md template).
FINAL WHOLE-BRANCH REVIEW (4b4504a..a1ebeda): READY TO MERGE, no Critical/Important. Go changes clean (nil-safe, disagg bool correct, R4/R8/omitempty OK, behavioral tests). 2 Minor Python items FIXED in follow-up commit: build_summary empty-glob guard + regret_join join-key contract comment. regret_join windowing caveat documented in FINDINGS.
ALL 7 TASKS COMPLETE. Full sweep running in background (rag is slow; ~12 bakes + 120 replays). Re-run report.py + fill FINDINGS once sweep done.

SLO REVISION (2026-06-25, user-driven): default τ_ttft=500ms was unmeetable for RAG 60k prefills (degenerate metric + EDPP z_ttft term). Fix:
- RAG clients split into slo_class standard (vector-qa) + batch (doc-read). NOTE: "interactive" is NOT a valid slo_class (valid: critical/standard/sheddable/batch/background) — used "standard".
- Per-workload per-class SLO/τ in sweep.sh: rag {standard: ttft500ms/itl150ms, batch: ttft5s/itl200ms}; synth {batch: ttft2s/itl150ms}. Passed via --slo-ttft/--slo-itl + --edpp-tau-ttft-classes/--edpp-tau-itl-classes.
- report.py: per-(workload,class) SLO_TTFT_US/SLO_ITL_US; added slo_itl_attain column (decode-bound synth → ITL is the real SLO).
- slo_class change requires re-bake; old sweep killed, full sweep relaunched (persistent monitor b4vrnr36z).
PRELIM (old degenerate SLO, RAG only): never@4 best on TTFT p99; edpp best of disagg arms at 1P3D/2P2D (stays local); edpp pathological at 3P1D (piles local load on lone D). disagg_frac rises w/ load & P-count. Reason never wins: equal-hardware split strands decode-only nodes (tiny RAG decode) + halves prefill servers. To be re-evaluated under realistic SLOs + with synth.

=== PLAN 4 (2026-07-01): EDPP estimator-validation harness (Stage A) ===
Branch feat/edpp-estimator-validation, base a8c20ad. Plan: docs/superpowers/plans/2026-07-01-edpp-estimator-validation.md
NOTE: golangci-lint MISSING on this machine — using go vet + gofmt per task; run golangci-lint at CI/final gate.
Task 1: complete (commit 109537e, review clean — SPEC ✅ / QUALITY Approved, no Critical/Important)
  CARRY-FORWARD to Task 4 (Minors): (1) recordAdmissionTime only wired inside `if cs.sloFeedback != nil` OnAdmit closures — fine for Stage A (--pd-decider edpp sets sloFeedback) but if --pd-outcome-trace must work without EDPP, revisit. (2) localAdmitTimes init only inside PD-enabled block; SetRecordPDOutcomes(true) must not be enabled outside a PD deployment (nil-map risk).
Task 2: complete (commit 5189e64, review clean — SPEC ✅ / QUALITY Approved)
  ⚠️ Minor (non-blocking): writer test is single happy-path; multi-record caller-order ("no sort") contract is covered downstream by Task 4's determinism test. record.go/pd_outcome_csv.go doc comments forward-reference Task 3/4 artifacts (intentional).
Task 3: complete (commit c26dd11, review clean — SPEC ✅ / QUALITY Approved). Disaggregated derived in builder (no ParentRequest field); tests assert sorted order, computed disagg, t_adm values + causality law.
  RESOLVED by controller (reviewer ⚠️ on metric keying): projectPDMetrics() (cluster.go:882, run-end) RE-KEYS per-request E2E/TTFT/ITL from sub-req ids to PARENT id (m.Request*[pid]). So realized metrics ARE keyed by the parentRequests map key. No gap.
  CARRY-FORWARD to Task 4 (ordering): BuildPDOutcomeRecords MUST be called AFTER projectPDMetrics has finalized metrics (i.e., at run end on final metrics) — else disagg records get zero metrics.
  Known limitation (documented): LocalTAdm always 0 (local absolute enqueue instant not tracked without flow control); downstream analysis must not treat it as meaningful.
Task 4: complete (commit 221d895, review clean — SPEC ✅ / QUALITY Approved, no findings)
  Shared helper writePDOutcomeTrace (root.go:1082) called from run (root.go:2170) + replay (replay.go:730); both pass cs.AggregatedMetrics() (finalized, projectPDMetrics-mutated in place at cluster.go:882 — ordering constraint satisfied). Flag shared via registerSimConfigFlags (no double-register). Zero-cost when unset (verified). Parity test builder-level only.
  ⚠️ Minor (final review): no automated CLI-level run==replay CSV parity assertion (builder-level determinism test only).
  *** SIGNIFICANT PRE-EXISTING FINDING (NOT introduced here; reproduces WITHOUT the flag; OUT OF SCOPE for this diff — confirmed by reviewer): PD+EDPP REPLAY completes only ~23/50 requests vs run 50/50. So replay-side outcome CSV realized/completed columns are NOT trustworthy for PD+EDPP until fixed. Admission-time columns match run byte-for-byte. AFFECTS bake-and-replay study methodology (edpp-study bakes once + replays across cells). Stage A anchor (Task 6) uses `blis run` directly, so anchor is unaffected. RAISE TO USER + separate investigation.
Task 5: complete (commit 0385132, review clean — SPEC ✅ / QUALITY Approved). Go-bool parse fix (parse_go_bool) applied to completed+disaggregated (draft's `== True` matched zero rows); div-by-zero/inf/empty guards present; e2e-tested on synthetic CSVs (total5/completed4/truncated1, disagg ratio 2.0 via ttft_p, local 1.279 via ttft_d).
  ⚠️ Minor (final review): truncated_or_dropped denominator assumes every outcome row has a decision match; add a warning if len(df)!=len(out) (join-loss unaccounted). Non-blocking; anchor run has total join match.
Task 5 FIX: empty-class grouping (commit 235c9a5) — local rows (empty slo_class→NaN) were silently dropped by groupby; now bucketed class=unknown + join-loss warning. Recovers ttft_d (local-path) validation.
Task 6: complete (commit 2634c98, FINDINGS.md). ANCHOR RAN SUCCESSFULLY (blis run bake + replay 2P2D edpp, synth2.0, 5000 reqs, disagg 4545/local 455). Harness works end-to-end.
  RESULT: ttft_p disagg median 2.08×; ttft_d local median 2.74× but tail p90 324s vs 0.55s (~590×) = archived HOL-blind reproduced; decode-admission (waiting-only) median ~905× under-pred (0.31s vs 285s) = Stage C case. prefill-adm 1.33×.
  DOWNGRADED the Task-4 "replay 23/50" finding: on full 5000-req config replay completes 4545/5000 (2-decode saturation, expected) — NOT a parity/engine bug; 23/50 was small-config horizon artifact. Admission columns match run byte-for-byte.
  FOLLOW-UPS (in FINDINGS): (1) local rows lack slo_class/input_tokens (no ParentRequest) — analysis buckets class=unknown; proper fix populates from original request stream. (2) `completed` proxy (E2E>0) = 5000 vs sim 4545 — looser than sim completion. (3) decode-side TTFT understated (realized decode T_adm 285s vs TTFT 120ms) — orthogonal known issue.
ALL 6 TASKS COMPLETE. Final whole-branch review next.
FINAL WHOLE-BRANCH REVIEW (opus, a8c20ad..2634c98): READY TO MERGE. No Critical/Important. Verified: no sim behavior change (edpp.go/edpp_coeffs.go untouched); zero-cost-when-unset; data-path keying (AggregatedMetrics post-projectPDMetrics, parent-keyed); INV-5/6/13 hold. Graceful degradation for non-EDPP PD deciders (zero columns, not error) — acceptable.
  Acceptable follow-ups (post-merge, non-blocking): (1) end-to-end CLI run==replay CSV-diff test; (2) populate slo_class/input_tokens for local rows from original request stream; (3) reconcile `completed` proxy (E2E>0) with sim completed_requests; (4) CI MUST run golangci-lint (missing locally).
PLAN 4 COMPLETE (a8c20ad..2634c98, branch feat/edpp-estimator-validation, 7 commits). NOT pushed/PR'd.
REPRO RECORDED (commit c3db115): campaigns/edpp-study/repro_stage_a.sh (tracked, runnable) bakes+replays+analyzes → out/stage_a/bias.json. Verified byte-identical to manual anchor (~110s). FINDINGS "Reproduction" block has checkpoint numbers (2.085 / 904.8 / 1.326 / 2.740). Findings + repro fully recorded in tracked files (FINDINGS.md + repro_stage_a.sh); code branch feat/edpp-estimator-validation NOT pushed/PR'd (WIP, no destination yet).

=== PLAN 5 (2026-07-02): EDPP work model correction + per-request validation (Stage B) ===
Branch feat/edpp-estimator-validation, base <see below>. Plan: docs/superpowers/plans/2026-07-02-edpp-work-model.md
DECISION (user-approved): W_p matches ACTIVE latency model (trained-physics basis, +a_p/2), NOT §3.6's causal −a_p/2. Trained-physics over-counts causal attention ~3× (roofline is causal); documented as deferred latency-model fidelity gap (spec §7). NO byte-identical synth regression — decision shift expected. golangci-lint still MISSING locally (go vet+gofmt).
Task 1: complete (commit 3c28373, review clean — SPEC ✅ / QUALITY Approved). Wp=C_pf·a_p+C_attn·a_p·(a_r+a_p/2); Wd discrete sum; both call sites pass a_r; Wp(100,100) test 3100→8100. No refit.
  Minor (final review): (1) stale arithmetic comments in sim/edpp_test.go:888,915 (wd=2148/qd=4148 from old deltaBarDecode path; tests still pass via loose assertions) — update comments. (2) deltaBarDecode now has no production caller (intended; still unit-tested).
Task 2: complete (commit 453f56e, review clean — SPEC ✅ / QUALITY Approved). WorkTraceRecord (12 fields) + WriteWorkTraceCSV (no sort), mirrors pd_outcome_csv.go.
  Minor (final review): writer test only pins first 7 columns; trailing 5 float columns' order unverified by unit test (covered end-to-end by Task 5/6). Pre-existing gofmt flag on untouched sim/trace/trace.go (not ours).
Task 3: complete (commit 159fd86, review clean — SPEC ✅ / QUALITY Approved). Per-request work accumulator in Simulator; prefill uses FULL si (float s/2.0, matching trained-physics MODEL not the recorder's int nt/2 — correct for closed-form validation); decode C0+C1·ProgressIndex; zero-cost when disabled; classification matches recorder. SetWorkTrace/WorkAccumulators wired in Task 4.
Task 4: complete (commit cfa6523, review clean — SPEC ✅ / QUALITY Approved, no Critical/Important). --edpp-work-trace flag on run+replay via shared writeWorkTrace helper (INV-13); BuildWorkTraceRecords correlates prefill/decode sub-reqs to parent via parentRequests + claimed map; closed forms with realized ap/ar/o; sorted by request_id. coeffs=config.EDPPCoeffs; instances via cs.instances/inst.sim/inst.id.
  Run/replay decode divergence (o_r/realized_decode) = pre-existing DES issue (hits pd-outcome identically), OUT OF SCOPE — does NOT break Stage B exactness (each execution internally consistent: realized work + closed form use same realized o_r).
  Minor (final review): (1) local-fallthrough relies on claimed map, not suffix exclusion — correct under "parent registered before sub-req runs" invariant; add a one-line comment. (2) vestigial workByInstance field (test-only holder).
Task 5: complete (commit 2f1051f, review clean — SPEC ✅ / QUALITY Approved). work_model_validation.py: single-chunk/chunked prefill split, all-rows decode, exact-0 gates, div0/inf/empty guards, lazy matplotlib. NOTE: brief's checkpoint "median_wp_over_old ≈1.53" was MY arithmetic error; correct value = 2.022 (median of 2.976, 1.068). Script correct; reviewer independently confirmed. Fix Task 6 checkpoint expectation to 2.022.
  Minor: prefill_chunks==0 falls in neither group (unreachable); lambda-assign nit.
Task 6: complete (commit ec1871b). repro_stage_b.sh + FINDINGS "Stage B". VALIDATION RAN (synth+rag @2P2D rate2.0, 5000 each):
  MODEL EXACT — single-chunk prefill max_abs_rel_err 5.6e-16 (synth) / 5.8e-16 (rag); decode 1.2e-15 (rag, all 5000) / synth median0 p90 2.5e-4 (small preemption tail). Chunked prefill residual = documented Σs² term (synth n=299 max0.28; rag n=3582 max0.22).
  Surprise: synth is NOT no-cache (median cache_hit_frac 0.91); rag 0.18. Doesn't affect exactness (accumulator+closed form both use realized a_p). correction median wp/old ~1.04 (linear term dominates; 3× basis change only at a_p≈a_r).
  §7 fidelity gap documented in FINDINGS. Decision-shift quantification deferred (needs pre-Task-1 rebuild; not the correctness gate).
ALL 6 STAGE B TASKS COMPLETE (c3db115..ec1871b). Final whole-branch review (Stage B delta) next.
STAGE B FINAL REVIEW (opus, c3db115..ec1871b): READY TO MERGE. No Critical/Important. Verified: accumulator == model StepTime classification (float-exact); INV-9 (decider input-side only); correlation total/no-leak; INV-6 sort; INV-13 shared write path; zero-cost off. Minors all acceptable follow-ups: stale edpp_test.go comments; dead deltaBarDecode; writer test pins 7 cols; vestigial workByInstance; py nits. CI must run golangci-lint.
PLAN 5 (Stage B) COMPLETE (c3db115..ec1871b, 6 commits). Branch feat/edpp-estimator-validation now = Stage A (7) + Stage B (6). NOT pushed/PR'd (WIP).

=== PLAN 6 (2026-07-02): EDPP occupancy-aware admission estimator + fidelity ablation (Stage C) ===
Branch feat/edpp-estimator-validation, base ad3acc6. Plan: docs/superpowers/plans/2026-07-02-edpp-admission-estimator.md
Spec: docs/superpowers/specs/2026-07-02-edpp-admission-estimator-design.md. 8 tasks.
Key: pluggable estimator (waiting/little/fluid/rollforward + oracle logging-only variants), replaces qD/muDec & qP/muPf admission terms; default waiting=byte-identical; INV-9 guard rejects oracle as routing driver; T1(single decode,never)/T2(1P1D,always) forced-routing microbenchmarks log all 6 variants; error decomposition realized−oracle(form) vs oracle−N̂(prediction). Layer-2 closed-form = SEPARATE next track.
NOTE: specs/plans/ledger now TRACKED on branch (force-added). golangci-lint still missing locally.
Task 1: complete (commit 1d0b7f8, review clean — SPEC ✅ / QUALITY Approved). AdmissionContext+RunningReqState+AdmissionDelayEstimator iface+waiting impl; seam swaps qD/muDec & qP/muPf only; default byte-identical (clampMu≥1e-3 so guard never fires); factory errors on unknown; constructor panics per R3.
  Minor: Name() method added beyond single-method iface (useful, harmless); waiting Mu<=0 guard unreachable on current clamped path (intentional).
Task 2: complete (commit 0dc7ada, review clean — SPEC ✅ / QUALITY Approved). little estimator = QueueDepth/AdmissionRate, guard rate<=0→0; case "little" in factory. Mirrors waiting pattern.
Task 3: complete (commits b769254..44065d0, review clean — SPEC ✅ / QUALITY Approved). RoutingSnapshot enriched (RunningDecode/RemainingDecodeWork/AdmissionRate); Decide fills both context literals; oracle-gated TrueRemaining (INV-9); zero-cost when detail off. FIX 44065d0: DispatchRate was structurally 0 in routing path (#1382) → re-wired snap.DispatchRate=inst.LatencyStats().DispatchRate into buildRouterState GATED on admission-detail flag; AdmissionRate = explicit-field > DispatchRate/1e6 > 0. little now functional.
  Minor (final review / downstream): (1) KVBlocks=⌈ProgressIndex/blockSize⌉ = total-seq blocks not decode-only (fine for rollforward, name may mislead). (2) DispatchRate 0 until first completion → little inactive during warmup. (3) ReqKVNeed uses len(InputTokens) (prefill need, applied to decode ctx too). (4) no PD integration run yet — Task 8 microbenchmark is the empirical check.
Task 4: complete (commit 9be5533, review clean — SPEC ✅ / QUALITY Approved). fluid estimator: free-slot→0; else N_ahead(=1)/X̂_dep, X̂_dep=BatchSize/(RemainingStepsEst·TIter). Test incl. full-batch/zero-waiting→~5000µs where waiting=0 (the bug). Minor: fluid models only slot-freeing (nAhead=1 hardcoded), not KV-driven admission — comment overpromises; documented simplification.
Task 5: complete (commit ac54d6e, review clean — SPEC ✅ / QUALITY Approved). rollforward: per-req departStep=TrueRemaining≥0 else max(RemainingStepsEst,1); sort.SliceStable asc; walk freed slots+KV; return firstDepart·TIter, cap at last; tests 3000µs (oracle) + 4000µs (est). Minor: departStep·TIter step-vs-elapsed (per brief); int64() floors est; cap/multi-departure branches untested (Task 8 exercises).
Task 6: complete (commit 7262fae, review clean — SPEC ✅ / QUALITY Approved). fluid_oracle/rollforward_oracle registered (reuse impls, prefer TrueRemaining); IsDeployableEstimator truth table; INV-9 guard in NewEDPPDecider panics on non-deployable routing driver (oracle still constructible directly for logging).
  Minor (final review): (1) guard panic path in NewEDPPDecider not exercised by a recover-test (INV-9 load-bearing — consider adding). (2) fluid_oracle untested e2e. (3) panic-vs-logrus.Fatalf: correct for library code.
Task 7: complete (commits 45a8ff3 + fix 0fe035f, review SPEC ✅ / QUALITY approved-after-gofmt-fix). --edpp-admission-trace (AdmissionRecord + WriteAdmissionCSV, 9 cols, no sort); local_t_adm now real (local enqueue captured at InjectRequestOnline); Decide attaches AdmissionCtxDecode/Prefill to decision (gated SetCaptureAdmissionContext, decision unaffected); BuildAdmissionRecords runs all 6 estimators per request, sorted; run+replay shared writeAdmissionTrace (INV-13). Renamed legacy AdmissionRecord→AdmissionDecisionRecord (complete). FIX 0fe035f: gofmt cmd/root.go + sim/trace/trace.go (from the rename).
  Minor (final review): (1) disagg row path unit-tested only (all-local smoke) — Task 8 T2 exercises live. (2) BuildAdmissionRecords p() maps estimator-construction error→0 (hardcoded-valid names, won't fire). (3) guard panic path still lacks recover-test (Task 6 minor).
Task 8: complete (commit 7725726). repro_stage_c.sh (T1 force-local via c-xfer 100s / T2 force-disagg c-xfer 0s, edpp 1P1D) + admission_ablation.py + honest FINDINGS "Stage C".
  RESULT: HEADLINE VALIDATED — T1 local (ttft_d, saturated p50 560s): waiting 57× under-pred → rollforward 1.29× near-exact (N̂ error ~0). The 905×-analog fixed by rollforward on the local pool.
  OPEN ISSUES (documented, NOT clean monotonic ablation): (1) fluid anomalous (~1e6× under-pred — RemainingStepsEst/free-slot collapse, needs debug). (2) little inert (predicts 0 — AdmissionRate unavailable at decision despite Task-3 wiring; DispatchRate 0 until first completion). (3) prefill-pool estimators broken (only decode RunningDecode enriched; prefill context has no occupancy → ttft_p unvalidated). (4) rollforward OVER-predicts on disagg decode path (T2 0.41×) — 905×-analog lives on local path (T1), which it fixes.
ALL 8 STAGE C TASKS COMPLETE (code 1d0b7f8..7725726). Infra merge-ready; fluid/little/prefill = follow-ups before paper ablation figure. Final whole-branch review next.
STAGE C FINAL REVIEW (opus, ad3acc6..7725726): READY TO MERGE (as infrastructure). No Critical/Important, no blockers. Verified: default byte-identical (TAdmEstimator unset→waiting; clampMu makes guard dead-defensive), estimator replaces only 2 admission terms, INV-9 oracle guard, INV-6 sort, INV-13 shared write path, INV-7 Periodic tier, zero-cost off, AdmissionRecord rename consistent, local_t_adm INV-5-guarded.
  Deferred follow-ups (land 1-3 before paper fidelity figure): (1) fluid ~1e6× under-pred bug; (2) little predicts 0 (needs admission-rate signal); (3) prefill-pool enrichment (only decode enriched → ttft_p inert); (4) rollforward over-predicts disagg decode; (5) NO CLI flag to select TAdmEstimator (programmatic-only — add before Stage D deploys rollforward as driver, also exercises guard runtime path); (6) minors: guard recover-test, rollforward cap/multi-departure tests, KVBlocks approx.
PLAN 6 (Stage C) COMPLETE (infra). Branch feat/edpp-estimator-validation = Stage A(7)+B(6)+C(spec/plan+8 tasks+fixes). Headline: rollforward fixes 57×→1.3× admission under-pred on saturated local pool. NOT pushed (user: push after Stage C).

=== PLAN 7 (2026-07-05): Stage C fix-cluster ===
Branch feat/edpp-estimator-validation, base aa48ec0. Plan: docs/superpowers/plans/2026-07-05-edpp-estimator-fixcluster.md; spec: .../specs/2026-07-05-edpp-estimator-fixcluster-design.md. 7 tasks.
Fixes: (1) fluid wave mean-field; (2) censored per-request remaining-steps + MaxBatchSize; (3) windowed admission-rate (little); (4) oracle/deployable separation in BuildAdmissionRecords (THE confound fix); (5) prefill enrichment (ttft_p); (6) --edpp-tadm-estimator flag; (7) re-run de-confounded + FINDINGS. Censored floor LOCAL to Decide (nHatFor unchanged; W_d out of scope). Default waiting byte-identical.
Task 1 (fluid wave): complete (commit e2f26da, review clean — SPEC ✅/QUALITY Approved). waves=ceil((QueueDepth+1)/BatchSize); prior TestFluidEstimator expectation updated 5000→20000; math import added. Minor: guard branches untested.
Task 2 (censored remaining-steps): complete (commit 53be33d, review clean — SPEC ✅/QUALITY Approved). nHatEff=max(nHatFor.mean(), maxStepsDone); remStepsEst=mean of max(nHatEff−StepsDone_i,1); nHatFor untouched (W_d safe); MaxBatchSize=float64(inst.MaxBatchSize()) populated+gated. Minor: RemainingDecodeWork now dead-on-path (cleanup candidate); test comment re N̂_out default slightly off.
Task 3 (windowed admission-rate): complete (commit 7e3f9e0, review clean — SPEC ✅/QUALITY Approved). onAdmitInternal hook (fires all admissions) → recordAdmission (1s window, req/µs, gated); snap.AdmissionRate populated in cluster_event.go; Decide already prefers snap.AdmissionRate>0. INV-6/default-waiting preserved. Minor: recordAdmission nil-guard direction (latent, unreachable — harden if touched).
Task 4 (oracle/deployable separation — THE confound fix): complete (commit a4166f6, review clean — SPEC ✅/QUALITY Approved). stripOracle deep-copies Running, sets TrueRemaining=-1 for the 4 deployable variants; 2 oracle variants use raw ctx. Deep-copy verified no-mutation. Updated pre-existing _Local/_Disaggregated wantPreds helper — reviewer confirmed GENUINE (independently derives split; dedicated test has hardcoded oracle anchor ~2000). deployable rollforward now ≠ oracle.
Task 5 (prefill enrichment): complete (commit 300e42d, review clean — SPEC ✅/QUALITY Approved). Found prefill ctx was decode-contaminated; added RunningPrefill snapshot field + Simulator.RunningPrefillState() (gated) + prefill-specific prefillRemStepsEst; prefillCtx.Running=prefillSnap.RunningPrefill. Prefill estimators non-inert under oracle.
  CROSS-TASK INTERACTION (assess in Task 7): prefill "remaining" = InputLen−ProgressIndex is KNOWN (not a hidden var like decode o_r), so the oracle/deployable split is decode-specific. Currently prefill TrueRemaining is oracle-gated (→ collapses to 1 off-oracle) AND Task 4 strips it for deployable → deployable prefill estimate is weak/mis-handled. Decode headline UNAFFECTED. Candidate refinement: populate prefill TrueRemaining unconditionally (it's known) + don't strip prefill. Let Task 7 ablation reveal if prefill numbers are wrong.
Task 6 (--edpp-tadm-estimator flag): complete (commit 985b0cc, review clean — SPEC ✅/QUALITY Approved). Flag registered once in registerSimConfigFlags (run+replay); DeploymentConfig.EDPPTAdmEstimator→EDPPConfig.TAdmEstimator at cluster.go:448; default ""→waiting byte-identical; guard test (recover) confirms rollforward_oracle driver panics. Minor: panic-from-CLI UX (acceptable).
Task 7 (de-confounded re-run) — RAN, REVEALED FIX-CLUSTER INCOMPLETE (numbers below, NOT yet written to FINDINGS):
  T1 local (realized ~560s): waiting 57.3× | little nan(0) | fluid 1.16× ✓✓ | rollforward-deploy 2.70× | rollforward-oracle 997× (!)
  T2 decode: waiting 4.0× | fluid 0.34× | rollforward-deploy 0.66× | rf-oracle 215×
  FINDINGS:
  1. fluid (wave) FIXED — now the BEST estimator (1.16× local). Tasks 1+2 worked.
  2. rollforward has the SAME queue-ignoring flaw fluid had: models only time-to-FIRST-slot-free (min remaining·TIter), IGNORES QueueDepth → under-predicts under backlog; oracle worse (997×) because true min-remaining can be ~1. INVERTS expected ordering (mean-field fluid > "true" rollforward). FIX NEEDED: rollforward must roll through ⌈(QueueDepth+1)/BatchSize⌉ waves, not stop at first departure.
  3. little STILL inert (nan/0) — windowed AdmissionRate not reaching the estimate at decision.
  4. prefill STILL all-nan — the Task-5 prefill oracle-semantics interaction.
  STATUS: paused Task 7 (FINDINGS not rewritten — numbers still moving). Surfaced to user for decision on rollforward-queue fix + little + prefill (another round) vs reassess.
Task 8 (rollforward queue-waves): complete (commit c6347d0, review clean — SPEC ✅/QUALITY Approved). Admission condition freeSlots>=QueueDepth+1 (was >=1); exhaustion→fluid wave fallback ⌈(QueueDepth+1)/BatchSize⌉·R̄·TIter (BatchSize>0 guard). QueueDepth=0 reduces to old (3 existing tests unchanged); new deep-queue test →60000. Minor: BatchSize<=0 branch unreachable (per spec); wave fallback drops KV (spec-conformant).
Task 9 (little/prefill root-cause+fix): complete (commit 6654677, review clean — SPEC ✅/QUALITY Approved).
  little=0 was a REAL bug: buildPoolFilteredSnapshots (the path EDPP decode-first routing + prefill closure consume) never populated AdmissionRate/DispatchRate (unlike buildRouterState). Fixed (mirror, gated). little→non-zero 4681/5000 (T1).
  prefill=0 was NOT a bug (reviewer confirmed SOUND): prefill pool unsaturated (QueueDepth=0, free slot) → occupancy estimators correctly ~0; waiting live for 2086 rows. My earlier "prefill inert" alarm was a 5-row early-sample artifact. Oracle-semantics concern de-escalated (latent, not manifesting). Follow-up: prefill estimators unproven under a SATURATED prefill pool.
Task 7/FINAL re-run (all fixes): DE-CONFOUNDED RESULTS (FINDINGS committed 3634435):
  T1 local (realized ~560s): waiting 57.3× | little 1.30× | fluid 1.16× | rollforward 1.16× | rf_oracle 1.25×.
  fluid==rollforward (converge: rollforward deep-queue fallback = wave form). oracle≈deployable (censored N̂_out closed the gap). little now works. T2 decode/local slightly over-predict (0.34-0.35×, disagg admission small). prefill ~0 = correct physics (unsaturated). Retracted the oracle-fed 1.29×.
ALL FIX-CLUSTER TASKS COMPLETE (Tasks 1-9). Story now coherent: occupancy-blind 57×-1430× → occupancy-aware ~1.16×. Follow-ups: utilization sweep, prefill-saturating validation, prefill oracle-semantics nuance.
Task 10 (INV-9 routing-path oracle strip): complete (commit b3d5a6c, review SPEC ✅/QUALITY Approved). censorOracleRemaining deep-copies+strips TrueRemaining=-1 for decodeCtx.Running & prefillCtx.Running feeding the routing driver. Decode routing path now INV-9-clean. Default waiting byte-identical; logging-path stripOracle untouched (FINDINGS numbers stand).
  RESIDUAL (reviewer, IMPORTANT, NOT closed): prefillRemStepsEst (edpp.go:483) reads RunningPrefill.TrueRemaining which is ORACLE-GATED (simulator.go:239, populated only in oracle mode as inLen−ProgressIndex) → feeds deployable prefillCtx.RemainingStepsEst → prefill routing path still leaks oracle via RemainingStepsEst. Implementer's "prefill chunk not oracle" dismissal was wrong (it IS oracle-gated). = the prefill-oracle-semantics follow-up. CORRECT FIX (prefill/decode ASYMMETRY): prefill remaining (inLen−ProgressIndex) is KNOWN/deployable → populate UNCONDITIONALLY + do NOT censor prefill; decode remaining stays oracle-gated+censored. Latent (only manifests with fluid/rollforward driver + admission-trace on prefill); does NOT affect results (logging path) or default waiting.
Task 11 (prefill/decode oracle-semantics asymmetry): complete (commit 1e5e7a1, review clean — SPEC ✅/QUALITY Approved). 3 spots: (1) RunningPrefillState populates trueRemaining=inLen−ProgressIndex unconditionally (known, not oracle; still under recordAdmissionDetail gate); (2) Decide leaves prefillCtx.Running uncensored, decode still censored; (3) BuildAdmissionRecords strips only pool!="prefill". Decode UNCHANGED (oracle-gated+censored). INV-9 fully closed (prefill=known input length, no OutputTokens). Tests assert both sides. Minor: test fingerprints pool by KVBlocks (structural); setter-comment glance.
INV-9 ROUTING-PATH LEAK FULLY CLOSED (Tasks 10+11). Decode numbers unchanged (logging decode path untouched); prefill still ~0 (unsaturated, correct). Final whole-branch re-review next.
FIX-CLUSTER FINAL REVIEW (opus, aa48ec0..1e5e7a1): READY TO MERGE. Prior INV-9 NEEDS-CHANGES RESOLVED across all paths (routing censors decode Running via deep-copy; decode remStepsEst never reads TrueRemaining; constructor guard blocks oracle drivers; prefill asymmetry no decode leak — disjoint Running sources). De-confounded FINDINGS valid (BuildAdmissionRecords strips decode/local, not prefill — matches routing semantics). Constraints all met (default byte-identical, censored floor local, INV-6/7/13, zero-cost off).
  Follow-ups (non-blocking): dead BatchSize<=0 branch; wave fallback drops KV; recordAdmission nil-guard; RemainingDecodeWork dead field; guard panic-from-CLI UX; test fingerprints pool by KVBlocks; prefill estimators unproven under saturation; utilization sweep.
FIX-CLUSTER (PLAN 7) COMPLETE (aa48ec0..1e5e7a1, 11 commits). Headline: occupancy-blind waiting 57×-1430× → occupancy-aware fluid/rollforward ~1.16× (converge); little 1.3×; oracle≈deployable (censored N̂_out); INV-9 clean.

=== PLAN 8: utilization sweep (2026-07-06) ===
Task 1 (utilization_sweep.py analyzer + self-check): complete (commit 2393eb9, review clean — SPEC ✅/QUALITY Approved). Two Python files; scan-verdict (exit0 iff STABLE) + aggregate (ρ̂=responses_per_sec/λ*, drift=median(2nd half)/median(1st half) by numeric request_id after warmup, per-point verdict + estimator pass-through + ratio_meaningful floor). 3/3 self-checks pass. ⚠️ resolved: ablation.json/admission.csv schema matches admission_ablation.py + --edpp-admission-trace (confirmed by controller). Minors (non-blocking): None-sort crash only if ≥2 null ρ̂ (λ*≤0 path, never in practice); scan-verdict substring "STABLE" match; unused import sys.
Task 2 (repro_utilization_sweep.sh): SCRIPT complete (commit 1264a3c, review clean — SPEC ✅/QUALITY Approved). Deviation confirmed correct: blis stdout isn't pure JSON (has === Simulation Metrics === headers) → switched to --metrics-path for clean cluster JSON (the plan's confirm-in-situ item). λ*=0.75 (scan 0.1/0.25/0.5 STABLE, 0.75 first non-STABLE). Minors: fragile verdict signal (analyzer error reads as non-STABLE); 2>/dev/null hides stderr; temp specs clutter out-dir.
Task 2 SCIENTIFIC RESULT (blocks Task 3 FINDINGS — escalated to user): sweep ran clean, admission delay BOUNDED+STATIONARY across ρ̂ 0.5→0.95 (drift≈1.0), realized p50 grows gently 29ms→47ms. BUT below saturation ALL occupancy-aware estimators (fluid/rollforward/oracles) predict ≈0 (free-slot early-return fires — slot usually free below capacity) and waiting≈0 too (empty waiting queue) → NaN/astronomical ratios; little OVER-predicts ~20×. Only at the single OVERLOADED point (ρ̂=0.953) do fluid/rollforward engage and there OVER-predict ~90× (rf_oracle 1.41×). So the clean "fluid/rollforward ≈1× across load" curve does NOT appear. Interpretation: Stage C's 57×→1.16× win is specific to HEAVY overload (slot-wait dominates); below saturation realized admission delay is a ~30-47ms step-cadence/batch-formation floor the estimators structurally return 0 for (they model slot/KV availability, not the wait-for-next-FormBatch). Likely routing-irrelevant (floor << τ_ttft=2s) but a real modeling gap. AWAITING USER DECISION on framing/experiment refinement before writing FINDINGS (Task 3).
Task 3 (FINDINGS + README + checkpoint): complete (commit b1273c4, done INLINE by controller — judgment-heavy narrative). FINDINGS "Utilization sweep" section + README §7.7. User chose Option 1 (record + confirm floor) and asked WHY estimators don't predict the current-step wait. ANSWER (recorded): free-slot early-return (admission_estimator.go:66) returns 0 when a slot/KV is free — models slot/KV availability, not the time-to-next-FormBatch (residual decode step). CONFIRMED empirically: realized sub-saturation admission floor ≈ ITL ≈ T_iter (29↔27ms @ρ̂0.5 → 44↔60ms @ρ̂0.93), climbs with batch fill. Headline: STEP FUNCTION (small routing-irrelevant ~30-47ms floor below cliff, explosion above); Stage C 57×→1.16× is regime-specific; gap routing-irrelevant (floor ≪ τ_ttft=2s). No estimator code change.
UTILIZATION SWEEP (PLAN 8) FINAL REVIEW (opus, 20d3d37..b1273c4): READY TO MERGE. No Critical/Important. All contracts verified against unchanged code (CSV/ablation/file-naming), doc claims match code (free-slot early-return, ρ̂ def, stationarity), measurement-only/INV-6/INV-9 hold. Minors = accepted set + cosmetic line-cite (fixed in d9d2020: fluid :66 / rollforward :84). Headline finding: STEP FUNCTION — sub-cliff admission delay = small routing-irrelevant ~30-47ms floor ≈ one decode step (≈ITL), estimators predict 0 there (free-slot early-return); Stage C 57×→1.16× is heavy-overload-specific. PLAN 8 COMPLETE (commits 2393eb9, 1264a3c, b1273c4, d9d2020). No Go change; no estimator-code change.

=== PLAN 9: admission-delay floor (2026-07-06) ===
Task 1 (flooredTAdm + apply to little/fluid/rollforward): complete (commit 1f2d4bd, review clean — SPEC ✅/QUALITY Approved). flooredTAdm(est,ctx)=max(est,TIter), strict > so no-op when TIter<=est incl TIter<=0. All returns of little(2)/fluid(3)/rollforward(5) wrapped; waiting UNTOUCHED (byte-identical strawman). Oracles inherit via shared impls (confirmed: NewAdmissionEstimator maps fluid_oracle→fluidEstimator, rollforward_oracle→rollforwardEstimator). Two free-slot test assertions updated 0→1000 (intended). go test ./sim/ ok. Minor (non-blocking): TestLittleEstimator zero-rate=0 assertion silently depends on TIter==0 (optional comment).
Task 2 (re-validate + docs): complete (commit 234b13e, done INLINE by controller — the dispatched subagent kept returning without blocking on the ~20min background runs; controller ran both scripts in foreground-background). RESULT: sweep STABLE band floored estimators now 1.05-1.26× (was predict-0/NaN); waiting unfloored still ~0. Stage C overload: waiting 57.3× UNCHANGED, occupancy-aware ~1.24× (shifted from pre-floor 1.16× — NOT a leak: max(est,TIter) no-op for saturated rows; T1 aggregate includes sub-saturation ramp rows now floored to TIter, nudging median; per-row saturated preds identical). Documented the nuance honestly in FINDINGS. FINDINGS+README updated planned→done.
ADMISSION FLOOR (PLAN 9) FINAL REVIEW (opus, f4f217e..234b13e): READY TO MERGE. No Critical/Important. Constraints verified (waiting untouched/byte-identical; flooredTAdm correct lower bound + no-op when TIter<=est incl <=0; all little/fluid/rollforward returns wrapped; INV-6/9 clean). Docs honesty confirmed — occupancy-aware overload framed as SHIFTED 1.16→1.24 (not "unchanged"), reasoning sound (per-row saturated preds identical, aggregate nudged by floored sub-saturation ramp rows); only waiting labeled unchanged (57.3×). Minors (non-blocking): oracle inheritance not in diff (confirmed elsewhere: NewAdmissionEstimator maps oracle→shared structs); little zero-rate now floored (defensible, optional docstring note); docstring "already exceeded" wording cosmetic. PLAN 9 COMPLETE (commits 1f2d4bd, 234b13e). Floor = estimator-fidelity only, routing-irrelevant.

=== PLAN 10: counterfactual regret harness (2026-07-07) ===
Task 1 (fixed-plan decider + --pd-plan + PrefillPodHint wiring): complete (commit 99e6bf6, review clean — SPEC ✅/QUALITY Approved, reviewer traced wiring in-situ). FixedPlanDecider forces local→{DecodePodOverride:d}/disagg→{+PrefillPodHint:p}; total-plan panic (R1); reads only plan (INV-9). PrefillPodHint threaded: set at cluster.go:2447 (parent.PrefillPodHint) → honored in pd_events.go Execute (short-circuits scorer). --pd-plan on run+replay (INV-13), first switch arm so it beats --pd-decider (tagless-switch conversion, clean). E2E test on 2P2D asserts forced (d,p) took effect. Minors (non-blocking, for final review): (1) PrefillPodHint NOT validated against prefill pool (decode override IS guarded) — typo'd plan → confusing failure; symmetric guard suggested. (2) trace suppressed on hint path (acceptable). (3) CSV loader duplicate/short-row tolerance (harness-generated inputs).
Task 2 (counterfactual driver): complete (commit 0cc29a7, review clean — SPEC ✅/QUALITY Approved). capture-plan (outcome→plan CSV, disagg→prefill_instance else local) + regret (max_action goodput−baseline, floored ≥0, on TOTAL goodput; frac_positive/total/mean). goodput() averages per-instance array. Filename regex dev_(.+)_([^_]+)\.json handles underscore reqids (request_5 → rid=request_5, action=M1-local) — reviewer traced correct. Minors (non-blocking): (1) missing baseline.json → ugly traceback (no clean msg); (2) test doesn't assert the underscore-reqid case (works, but unverified by test — Task 3's real request_N run exercises it e2e); (3) unused import sys.
Task 3 (repro_counterfactual.sh + diagnosis + findings): complete (commits 10e1d3c + fix 46e839b; review SPEC ✅, QUALITY Approved after fix). Self-consistency gate PASSED exact (replay slo_attainment 0.9775 == baseline). Reduced-EDPP 1P2D K=10 800-req: mean_regret 0.0139, total 0.1387, frac_positive 0.70. KEY RESULT: all positive regret concentrated on KEPT-LOCAL decisions (6 of them, 0.0225 each, recovered by pinning decode to instance_1); explicit disaggregation calls near-optimal. Reads as "reduced-EDPP under-specifies decode-node placement on the local path" (delegates d to scorer) — the exact choice the joint rule would own → empirical motivation for [C]. Diagnostic is LOCAL one-step deviation, not global optimum (stated). Review fix: FINDINGS table 5/5→6/4 (verified vs regret.json: 6×0.0225+0.0037=0.1387, 7 positive); filename underscore-free-action-token contract documented in driver docstring + README §7.8. Minor (non-blocking): cosmetic "x3 deviations" echo (n_runs true); local-deviation conflates should-disagg vs should-pin-decode (disclosed). make_specs.py emits synth_cf.yaml (800-req saturating, gitignored).
CF-REGRET (PLAN 10) FINAL REVIEW (opus, f1177a8..46e839b): READY TO MERGE. No blockers. All constraints verified (decider semantics/total-plan-panic/INV-9; --pd-plan precedence + run+replay INV-13; PrefillPodHint threaded decision→parent→prefill-event; self-consistency gate loud; INV-6 seeded). Three pieces cohere (plan CSV format decider↔capture-plan↔repro). FINDINGS 6/4 numbers consistent. Carried Minors all judged acceptable follow-ups (tool plans machine-generated). Cosmetic: softened "All"→"nearly all" regret-on-local (commit after review). PLAN 10 COMPLETE (commits 99e6bf6, 0cc29a7, 10e1d3c, 46e839b, + doc nit).

=== PLAN 11: joint mechanism sub-project 1 (2026-07-08) ===
Task 1 (per-instance Q_i qByInstance): complete (commit 31fcfe8, review clean — SPEC ✅/QUALITY Approved). instWork/drainInst mirror the pool wp/wd exactly; pool qpWork/qdWork byte-identical; sums==pool invariant test passes; ""-nil-safe symmetric (add+drain both skipped, no drift); OnComplete correctly not mirrored (waiting-only migration doesn't drain pool there). OnRoute extended (decodeInst,prefillInst); 18 callers updated with ,"","". CARRY-FORWARD to Task 2: single live call site cluster.go:2340 passes decodeInst but prefillInst="" (prefill chosen later at PrefillRoutingEvent pd_events.go:56). JOINT path fix: the OnRoute call must pass prefillInst = disaggDecision.PrefillPodHint (the argmin's p*, known at Decide in joint mode) so per-instance q_p populates for the congestion term. Minors (non-blocking): drain-cycle sum==pool not asserted (add a test); zero-valued map entries linger after drain.
Task 2 (--edpp-joint Decide): complete (commits 2996079 + fix e8c47fe; review SPEC ✅/QUALITY Approved). Joint decideJoint enumerates sorted (d,p), normalized J (all /τ divisors present, absolute per-candidate T̂ not difference), per-candidate cache-aware apForInstance (decode cache for local, prefill node cache for disagg), deterministic tie-break (lowest ID, local-first), per-instance q from qByInstance, prefillInst=PrefillPodHint threaded into OnRoute. Reduced path BYTE-IDENTICAL (exact-value trace tests unchanged; only flag-guard + 3 value-preserving extractions: apForInstance/decodeRemStepsEst/prefillRemStepsEst/transferPenalty). §5.5 reduction verified algebraically (J_local−J_disagg == reduced lhs−rhs). --edpp-joint on run+replay; INV-9 clean. IMPORTANT FIX (e8c47fe): added m_dec(d)=deltaBarDecode base-decode ITL term to J (was omitted); argmin-invariant under homogeneous θ (all joint+reduction tests unchanged), included for §3 fidelity + heterogeneity-readiness; + TestJoint_DisaggToWarmPrefillNode locks prefill-node a_p sourcing. NOTE: fix subagent over-committed ~5000 lines of untracked scratch; controller reset+re-committed only the 2 sim files. Minors (non-blocking, final review): maxInt redundant guard; DRY mirror of AdmissionContext assembly (justified — reduced no-snapshot fallback differs); empty-prompt joint has no keep-D early return (intentional). ⚠️ §5.5 equivalence exercised only cold-cold + now warm-prefill; θ_i plumbing into deltaBarDecode needed in sub-project 2.
Task 3 (scorer-vs-joint divergence log): complete (commit d4a64d5, review clean — SPEC ✅/QUALITY Approved; committed only 10 intended files, no scratch). --edpp-joint-trace CSV (mirrors edpp-decision-trace; requires --trace-level decisions). Logs scorer_d(=SelectedInstance)/joint_d/joint_p/scorer_p(shadow)/agree_d/agree_p/J_scorer/J_joint. Shadow prefill scorer on dedicated RNG (edpp-joint-shadow-prefill) → routing unaffected, INV-6 byte-identical on/off. jointCandidateCost extracted + shared by argmin AND shadow-eval (no J drift). J_joint<=J_scorer invariant tested. Pure post-hoc instrumentation (record built after argmin picks best). Reduced path untouched. Minors (non-blocking): J_scorer falls back to local slice if shadow prefill pick unresolved (could mislead analyst — consider "unresolved" flag); flag-help omits --trace-level decisions dependency.
Task 4 (exploratory sweep + findings): complete (commit f34ef1a + K-fix; review SPEC ✅/QUALITY Approved). 3 correctness gates PASS: (a) --edpp-joint OFF byte-identical (det check), (b) §5.5 reduction test, (c) joint-plan self-consistency (0.97875==0.97875, plan built from --edpp-joint-trace since --pd-outcome-trace leaves decode_instance empty for local — workaround SOUND, gate real). repro_joint.sh (1P2D,2P2D × synth_cf,synth_asym; joint vs reduced, rollforward driver) + joint_divergence.py (+self-test). HONEST RESULT: joint does NOT uniformly win — 1P2D synth_cf (cache-uniform) reduced regret 0.030→joint 0.0225 (~25% cut, goodput 0.990→0.979); cache-asym reduced 0.000→joint 0.0012 (ties-to-loses, reduced already optimal at loose batch SLO); prefill lever fires only in 2P2D asym (p-divergence 0.26). θ_i heterogeneity win deferred (sub-project 2). Minors (non-blocking, final review): gate(a) is a determinism check not pre-joint equivalence (§5.5 carries algebraic equiv; FINDINGS wording slightly overstates); divergence direction J-level only (trace lacks occupancy/a_p); per-cell self-consistency WARN-only in sweep; K default fixed 6→4. ⚠️ FINDINGS/README reference paper-figure scripts (repro_work_model_sweep.sh etc.) NOT in this commit (untracked scratch) — dangling refs, confirm they land separately. ⚠️ populate decode_instance for local in --pd-outcome-trace. ⚠️ tighter SLO for synth_asym to see P-reroute goodput.
JOINT MECHANISM (PLAN 11, sub-project 1) FINAL REVIEW (opus, d34367c..f70e023): READY TO MERGE (code). All invariants verified across the delta: reduced byte-identical (joint gated by early-return; shared helpers apForInstance/decodeRemStepsEst/prefillRemStepsEst/transferPenalty algebraically identical; qByInstance written-not-read on reduced); normalized J matches §5.3 (absolute per-candidate T̂, two documented homogeneous deviations); override applied BEFORE OnRoute attribution; divergence trace pure/RNG-isolated, J_joint<=J_scorer; INV-6/9/13. FINDINGS honest (joint does NOT uniformly win). All carried Minors = acceptable follow-ups. ONE DOC-INTEGRITY FLAG (not code-blocking, user's separate workstream): README §2 "Paper-figure scripts"/FINDINGS "Estimator-accuracy figures" reference repro_work_model_sweep.sh, repro_ttft_d_local.sh, analyze/work_model_sweep.py, ttft_d_local.py which are UNTRACKED at HEAD → README falsely marks "tracked" + reproduce commands fail on clean clone. USER DECISION: commit those 4 scripts, or drop the paper-figure doc section. NOT touched (user's intentional README edits + their untracked scripts).
PLAN 11 COMPLETE (commits 31fcfe8, 2996079, e8c47fe, d4a64d5, f34ef1a, f70e023). Joint mechanism sub-project 1 done: --edpp-joint works, modest workload-dependent win on homogeneous HW (recovers ~25% decode-placement regret cache-uniform; ties/loses cache-asym), θ_i heterogeneity win = sub-project 2.

=== PLAN 12: wire hw_config_by_gpu into bundle (T-A step 1) (2026-07-12) ===
Task 1 (PolicyBundle.HWConfigByGPU + validation): complete (commit 60dcdaa, review clean — SPEC ✅/QUALITY Approved; only sim/bundle.go + bundle_test.go, no scratch). HardwareCalibBundleConfig (yaml tags tflops_peak/tflops_fp8/bw_peak_tbs/mfu_prefill/mfu_decode/memory_gib) + PolicyBundle.HWConfigByGPU (yaml hw_config_by_gpu, nil when omitted, backward-compat). Validate() rejects tflops_peak/bw_peak_tbs <=0 or NaN (!(x>0)) with hw_config_by_gpu[%q] msg. Round-trip + reject tests real. CARRY-FORWARD to Task 2: (a) LoadPolicyBundle does NOT auto-call Validate() — Task 2 must ensure the CLI invokes bundle.Validate() so bad hw configs fail fast (node_pools validation is in the same Validate(), so the CLI likely already calls it — CONFIRM). (b) conversion copies MFU/FP8/memory as-is (unvalidated by design).
Task 2 (wire bundle hw_config_by_gpu -> DeploymentConfig.HWConfigByGPU): complete (commits 60dcdaa..b5c5269, review clean). cmd/root.go: hwConfigByGPUFromBundle helper (field-by-field copy, nil when empty -> backward-compat) + HWConfigByGPU: bundleHWConfigByGPU in the DeploymentConfig literal next to NodePools. replay skipped (node-pools replay-fatal, INV-13). CLI calls bundle.Validate() at resolvePolicies (carry-forward (a) closed). Task-review fix: first integration test was vacuous (didn't guard the literal line); replaced with TestHWConfigByGPU_RunCmdLiteralWiring_PerPoolSpeedObservable (re-exec blis run, two-pool H100+A100, crippled-A100 vs baseline completed_requests) verified RED when the line is removed, GREEN when present; production code byte-identical. Minor (final-review triage): the guard's "last stdout match = cluster aggregate" is an implicit ordering dependency (documented in-code, not structurally asserted).
Final whole-branch review (2b49311..b5c5269): READY TO MERGE, no Critical/Important. Two Minors closed in b07a79f: (1) converter test now asserts all six fields incl TFlopsFP8/MemoryGiB; (2) configuration.md documents that a per-GPU hw_config_by_gpu entry fully REPLACES (not merges) the CLI --hardware calib.
PLAN 12 COMPLETE (commits 2b49311..b07a79f). hw_config_by_gpu now wired end-to-end; heterogeneous serving is configurable. Branch feat/edpp-estimator-validation, not yet pushed.

=== PLAN 13: per-instance θ_i in the EDPP joint decider (2026-07-14) ===
Plan docs/superpowers/plans/2026-07-14-edpp-per-instance-theta.md (commit 5520627). 8 tasks. Base for Task 1 = 5520627. Executing subagent-driven.
Task 1 (coeffs_by_gpu bundle field + validation): complete (commit 1731f48, review clean — SPEC ✅/QUALITY Approved). PolicyBundle.CoeffsByGPU map[string]string (yaml coeffs_by_gpu), nil when omitted; Validate() rejects empty/whitespace paths (strings.TrimSpace) with coeffs_by_gpu[%q] msg, structural-only (no I/O). 3 tests via writeTempYAML+LoadPolicyBundle. MINOR (final-review roll-up): RejectsEmptyPath only tests "" not whitespace-only (guard handles it; matches brief).
Task 2 (populate RoutingSnapshot.GPUType): complete (commit c164502, review clean — SPEC ✅/QUALITY Approved; reviewer reverted the line to confirm red). One line snap.GPUType = inst.GPU() in buildPoolFilteredSnapshots (sim/cluster/cluster.go:1249), mirrors buildRouterState. Test TestBuildPoolFilteredSnapshots_CarriesGPUType (admission_trace_test.go) via newTestEDPPDeploymentConfig, asserts GPUType=="H100".
Task 3 (decider θ store + coeffsFor + EDPPConfig.CoeffsByGPU): complete (commit 6d23935, review clean — SPEC ✅/QUALITY Approved; cost math untouched, full sim suite byte-identical). coeffsFor returns mapped θ when gpuType!="" and present, else d.coeffs (nil map / unmapped / "" all fall back). NewEDPPDecider validates each entry via EDPPCoeffs.validate() + panic (R3). MINOR (final-review roll-up): coeffsByGPU assigned by reference (not defensively copied like class-target maps) — spec-conformant, read-only config.
Task 4 (per-candidate θ in jointCandidateCost): complete (commit 6b1e3d1, review clean opus — SPEC ✅/QUALITY Approved; byte-identity + thetaD/thetaP keying + genuine selection guard all confirmed). jointEvalCtx wd/jDecodeITL -> nHatOut; wd/mDec moved into per-candidate cost via thetaD; chunkTerms(theta,ap); local uses thetaD, disagg thetaP; normalizer stays global. Tests in sim/edpp_joint_test.go. Fixture: slow θ scales AlphaP alongside AlphaD (validate >10% α-divergence guard). MINOR: byte-identity test is decision-level (existing joint goldens carry bit-identity).
Task 5 (reduced path decode-side θ): complete (commit 4bde35a, review clean — SPEC ✅/QUALITY Approved). thetaD=coeffsFor(decSnap.GPUType) on wp/muDec/tBminus1/itlTerm.CPf; deltaPfChunk SPLIT: deltaPfChunkLocal(thetaD)->ttftD, deltaPfChunk(d.coeffs)->ttftP; prefill-side (muPf/tIterPrefill) stays pool-aggregate d.coeffs. Byte-identical homogeneous. MINOR (roll-up): EDPPDecisionTrace.DeltaPfChunk reports only pool-aggregate value (debug visibility, out of scope).
Task 6 (wire coeffs_by_gpu -> decider): complete (commits e46a5d5 + fix db145ba, review clean opus — SPEC ✅/QUALITY Approved). DeploymentConfig.EDPPCoeffsByGPU (yaml edpp_coeffs_by_gpu) + edppCoeffsByGPUFromBundle (fail-fast load) + literal wiring + cluster.go:458 CoeffsByGPU: config.EDPPCoeffsByGPU. Loader unit tests. FIX: removed production test-seam edppCoeffsByGPUWiringHook (reviewer: HW precedent guards the same literal behaviorally with no seam) — literal's genuine guard DEFERRED to Task 8 (behavioral, differing coeffs). replay not wired (matches HWConfigByGPU precedent, node-pools replay-fatal). Verified: hook 0 matches, 4 wiring changes intact, full sim+cmd suite green.
Task 7 (offline θ_i extraction + slow-device coeffs file): complete (commit 38c9b70, review clean — SPEC ✅/QUALITY Approved). repro_theta_by_gpu.sh (CSV-tap route; feasibility probe passed, no fallback) + coeffs-llama70b-a100crippled-tp4.json (force-added, *.json gitignored). Fit R²≈1 (decode cond 2.21 / prefill 6.62), all coeffs 1.66-4.79× > H100 (genuinely slower), LoadEDPPCoeffs OK, no confound (bundle 400/0.7 = T-A convention). MINORS (roll-up): no --validate block; no bit-exact regeneration checkpoint. Follow-on cleanup: force-added the frozen H100 coeffs file too (commit d8e5419) to resolve tracking asymmetry + un-silence t.Skipf tests on fresh clones.
Task 8 (acceptance on saturating harness + FINDINGS): complete (commit f6ed51c, review clean opus — SPEC ✅/QUALITY Approved; guard verified LIVE RED/GREEN, negative result honestly recorded, over-correction diagnosis sound not a bug). THETA=1 mode in repro_hetero_hw.sh emits coeffs_by_gpu theta bundle; θ_i-joint arm. ACCEPTANCE VERDICT: BAR NOT MET but characterized — θ_i-joint over-corrects (95-97% fast on 2/3 seeds, goodput 0.685/0.750 below reactive baselines; seed42 partial win 0.877/80%); under-capacity unregressed (≡ homogeneous joint). Mechanism: 4.78× slow-A100 work term dominates reactive Q_i governor → needs deferred Z^I_i capacity coupling. FINDINGS "Per-instance θ_i (T-B) result" + TODO updated. Part C: TestCoeffsByGPU_RunCmdLiteralWiring_DecodeSplitObservable (behavioral, no production seam) RED-when-literal-severed verified. MINORS: §9 predicted undershoot, got overshoot (still a valid bound); overwrote unrelated task-8-report.md.
PLAN 13 all 8 tasks complete (commits 5520627..f6ed51c + d8e5419 H100-tracking). Proceeding to final whole-plan review.
FINAL whole-plan review (5520627..f6ed51c, opus): READY TO MERGE, no Critical/Important. End-to-end field plumbing coherent; INV-6 byte-identity (joint+reduced), INV-9, INV-13 (replay intentionally unwired, matches hw_config precedent), R8-R10 all hold; thetaD/thetaP no cross-wiring; coeffs provenance consistent (no confound); guards genuine; Task-6 production seam fully gone. 5 Minors all follow-ups (none block): (a) whitespace-path test coverage, (b) coeffsByGPU by-reference, (c) trace DeltaPfChunk pool-aggregate only, (d) no A100 --validate/regen-checkpoint, (e) §9 predicted undershoot got overshoot. Negative acceptance result honestly recorded, code correct.
PLAN 13 COMPLETE & MERGE-READY (commits 5520627..f6ed51c + d8e5419). NOT pushed/merged (awaiting user). Headline: per-instance θ_i built end-to-end; acceptance BAR NOT MET — θ_i over-corrects (necessary-not-sufficient), next = couple with deferred Z^I_i.

=== PLAN 14: least-ttft disaggregation baseline (2026-07-15) ===
Plan docs/superpowers/plans/2026-07-15-edpp-least-ttft-baseline.md (commit 44e4988). 2 tasks. Base for Task 1 = 44e4988. Executing subagent-driven. Purpose: isolate whether EDPP's prefill-bound win is drift-plus-penalty-specific or generic dynamic least-TTFT routing.
Task 1 (Rule field + decision branch): complete (commit 2c1183c, review clean — SPEC ✅/QUALITY Approved; guard verified non-vacuous by reviewer instrumentation: dpp flips false->true under z=1e12, least-ttft invariant). EDPPConfig.Rule + EDPPDecider.rule + validate({"","dpp","least-ttft"}) + single disagg flag used at BOTH decision(638) and trace(649); least-ttft branch reads only ttftP/ttftD. Byte-identical for Rule in {"","dpp"} (full sim suite green). MINOR (roll-up): least-ttft still computes the full machinery then discards it (wasted compute, not correctness).
Task 2 (--edpp-rule flag wiring): complete (commits 520a5c2 + fix 838f0ab, review clean — SPEC ✅/QUALITY Approved; reviewer negative-control-tested the reject = non-vacuous). Flag threaded cmd->DeploymentConfig.EDPPRule->EDPPConfig.Rule (both run AND replay); least-ttft+joint rejected via Fatalf. FIX: reviewer caught that the implementer's "mirrors EDPPJoint precedent" was wrong (EDPPJoint IS wired in replay) — the missing EDPPRule in replay was a NEW INV-13 silent-no-op; fixed in 838f0ab (EDPPRule: edppRule in replay.go, symmetric grep confirms). MINORS (roll-up): reject guard not gated on pd-decider==edpp (matches local convention); one t.Errorf vs t.Fatalf cosmetic.
PLAN 14 all tasks complete (commits 44e4988..838f0ab). Proceeding to final whole-plan review.
FINAL whole-plan review (44e4988..838f0ab, opus): NEEDS CHANGES -> one Important (least-ttft+joint reject missing on replay path). Scientific core CONFIRMED clean (baseline genuinely isolates EDPP-minus-machinery: identical ttftP/ttftD, only final comparison differs), byte-identity holds, trace consistent, run-reject works. FIX 9798bca: mirrored the reject into cmd/replay.go:259 (symmetric grep confirms; manual runtime check exit 1 + msg; test skipped — replay harness needs trace+coeffs fixtures, low-risk one-line mirror of an approved guard). 3 rollup Minors all accept-as-is.
PLAN 14 COMPLETE & MERGE-READY (commits 44e4988..9798bca). least-ttft baseline built; NEXT = spectrum experiment (never/always/least-ttft/edpp across decode-bound->prefill-bound) to determine paper framing.
