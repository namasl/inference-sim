---
schema: archwatch/3
arch_id: GLM-5.2
display_name: GLM-5.2
detected_at: '2026-09-03T22:27:04Z'
sources:
- inferencex
triggers:
- T5
- alias-join
markers:
- alias-join
join_edges:
- family:glm52
significance:
- S3
bucket: null
bucket0_failures: []
silent_failures: []
silently_wrong: false
known_arch_drift: false
unparsed_fields: []
est_total_params: null
est_active_params: null
corroborated: false
model_types: []
model_ids: []
orgs: []
urls:
  inferencex.commit: https://github.com/SemiAnalysisAI/InferenceX/commit/f1e2e19d727c736d359fef9ebe87a696af461165
  inferencex.commit.2: https://github.com/SemiAnalysisAI/InferenceX/commit/f2be63a98849591c40d80428cef5af6fb185417c
  inferencex.commit.3: https://github.com/SemiAnalysisAI/InferenceX/commit/e807a5283c95dfa725df13c94d349196aea57820
  inferencex.commit.4: https://github.com/SemiAnalysisAI/InferenceX/commit/3a7d6b714844a181a6a5ebdcfd4097ee04a0cf5e
  inferencex.pr: https://github.com/SemiAnalysisAI/InferenceX/pull/2720
  inferencex.pr.2: https://github.com/SemiAnalysisAI/InferenceX/pull/2808
  inferencex.pr.3: https://github.com/SemiAnalysisAI/InferenceX/pull/2773
  inferencex.pr.4: https://github.com/SemiAnalysisAI/InferenceX/pull/2777
has_config: false
has_perf_notes: true
perf:
- source: inferencex
  value:
  - hardware: mi355x
    config_key: glm5.2-fp4-mi355x-sglang-agentic-mtp
    precision: fp4
    framework: sglang
    scenario: agentic-coding
    pr: https://github.com/SemiAnalysisAI/InferenceX/pull/2777
    itl_pct_delta_p50: 5.0
    itl_ms_p50: 6.95
    itl_ms_p50_alt: 7.3
    interactivity_tok_per_s_per_user_p90: 110.5
    interactivity_tok_per_s_per_user_p90_alt: 105.0
    throughput_pct_delta: 12.0
    notes: 'Switch the TP8 arm from EP=8 to EP=1: removing the MoE all-to-all collective reduces ITL p50 by ~5% at conc 4 (6.95 ms vs 7.3 ms baseline) and raises interactivity P90 from 105 to 110.5 tok/s/user. Extend the TP8 concurrency sweep to [4, 6, 8, 10] to cover the mid-range crossover region. Raise HiCache ratio from 1.5 to 2.5 and switch write policy to write_through_selective (per PR #2679): the agentic-coding corpus saturates any fixed DRAM pool at conc ≥ 10; the larger host tier sustains throughput through conc 12 (+12% tok/s/GPU vs conc 10). Trim the TP4 concurrency list to [1, 2, 4, 8, 10, 12] removing the untested conc 16 point.'
  raw_ref: 3a7d6b714844a181a6a5ebdcfd4097ee04a0cf5e
stage2: pending
dry_run: true
---

# [archwatch] GLM-5.2 — new architecture detected

Stage-1 stub, generated by `archwatch` from deterministic checks only — no LLM ran, and nothing here was posted anywhere. Tracking only.

- **Verdict:** not checked — no `config.json` available yet
- **Architecture:** `GLM-5.2` (the pipeline's primary key)
- **Seen by:** InferenceX — single source

## Sources

| Source | Ref | Org | Evidence | Links |
| --- | --- | --- | --- | --- |
| InferenceX | `3a7d6b714844a181a6a5ebdcfd4097ee04a0cf5e` | — | InferenceX benchmarks GLM-5.2: perf-changelog entry in perf-changelog.yaml (keys: glm5.2-fp4-mi355x-sglang-agentic-mtp); 1 perf row(s) [commit 3a7d6b714] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/3a7d6b714844a181a6a5ebdcfd4097ee04a0cf5e) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2777) |
| InferenceX | `e807a5283c95dfa725df13c94d349196aea57820` | — | InferenceX benchmarks GLM-5.2: perf-changelog entry in perf-changelog.yaml (keys: glm5.2-fp4-gb300-dynamo-trt-agentic-agg-mtp, glm5.2-fp4-gb300-dynamo-trt-agentic-disagg-mtp) [commit e807a5283] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/e807a5283c95dfa725df13c94d349196aea57820) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2773) |
| InferenceX | `f1e2e19d727c736d359fef9ebe87a696af461165` | — | InferenceX benchmarks GLM-5.2: perf-changelog entry in perf-changelog.yaml (keys: glm5.2-fp4-gb300-dynamo-trt-agentic-disagg-mtp) [commit f1e2e19d7] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/f1e2e19d727c736d359fef9ebe87a696af461165) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2720) |
| InferenceX | `f2be63a98849591c40d80428cef5af6fb185417c` | — | InferenceX benchmarks GLM-5.2: perf-changelog entry in perf-changelog.yaml (keys: glm5.2-fp4-b200-sglang-agentic-mtp) [commit f2be63a98] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/f2be63a98849591c40d80428cef5af6fb185417c) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2808) |

**How these signals were joined** — union-find over architecture id, normalized repo id and normalized family name. Every source row above was merged into this one report because of these edges:

- `family:glm52` — normalized family name

> If any edge above is wrong, this stub has **fused two distinct architectures into one report** — worse than emitting two stubs, because the findings below then describe a model that does not exist. Check that every source row really is the same model before trusting the rest of this file.

## Why it fired

**Triggers** (any one is enough):

- **T5** — a curated benchmark / analyst entry names a model we had not seen

**Significance** (at least one must hold):

- **S3** — framework / analyst attention: a vLLM, SGLang or InferenceX signal

**Markers** (how this candidate was assembled, not why it matters):

- **`alias-join`** — no signal carried an `architectures[]` entry, so these signals were joined on a normalized display name — the `arch_id` above is that alias key, not a string read out of a config

## Deterministic findings (stage 1)

Computed by reading BLIS as text against the harvested support surface. No LLM, no simulation run — these are facts about the config, not a fidelity judgement.

### Bucket 0 — would BLIS accept this config?

**Not checked** — no `config.json` was obtainable for this architecture, so the validators could not run. Bucket is left undetermined.

### Config fields BLIS does not parse

_No config available, so no field diff could be computed._

### Parameter estimates

_Not estimated — the config lacked the fields the estimator needs._

## Reported performance numbers

Carried in a signal's `extra["perf"]`. Recorded verbatim — these are future validation ground truth for BLIS, not archwatch's own measurements.

**InferenceX** (`3a7d6b714844a181a6a5ebdcfd4097ee04a0cf5e`)

| `hardware` | `config_key` | `precision` | `framework` | `scenario` | `pr` | `itl_pct_delta_p50` | `itl_ms_p50` | `itl_ms_p50_alt` | `interactivity_tok_per_s_per_user_p90` | `interactivity_tok_per_s_per_user_p90_alt` | `throughput_pct_delta` | `notes` |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| mi355x | glm5.2-fp4-mi355x-sglang-agentic-mtp | fp4 | sglang | agentic-coding | https://github.com/SemiAnalysisAI/InferenceX/pull/2777 | 5.0 | 6.95 | 7.3 | 110.5 | 105.0 | 12.0 | Switch the TP8 arm from EP=8 to EP=1: removing the MoE all-to-all collective reduces ITL p50 by ~5% at conc 4 (6.95 ms vs 7.3 ms baseline) and raises interactivity P90 from 105 to 110.5 tok/s/user. Extend the TP8 concurrency sweep to [4, 6, 8, 10] to cover the mid-range crossover region. Raise HiCache ratio from 1.5 to 2.5 and switch write policy to write_through_selective (per PR #2679): the agentic-coding corpus saturates any fixed DRAM pool at conc ≥ 10; the larger host tier sustains throughput through conc 12 (+12% tok/s/GPU vs conc 10). Trim the TP4 concurrency list to [1, 2, 4, 8, 10, 12] removing the untested conc 16 point. |

## Third-party mechanism notes (verbatim)

Quoted from the source's own changelog or write-up — **their words, unverified, not archwatch's analysis.** Notes like these routinely name a mechanism days before a config is public, so read them as the earliest available hint about what stage 2 will have to model.

**InferenceX** (`f1e2e19d727c736d359fef9ebe87a696af461165`):

> Add a GLM-5.2 concurrency-1 AgentX point using a 1-prefill/1-decode disaggregated topology with TP4 attention-DP prefill and TP8 decode.

> Use MTP depth 5 with the committed golden acceptance target, tensor-parallel MoE on decode, and the CUTEDSL top-k path.

> Align recipe KV offloading with the master config and keep the benchmark client colocated with the frontend orchestrator.

> Add the new concurrency-1 topology to the existing disaggregated configuration key while retaining all published points.

> Enable TensorRT-LLM iteration statistics and require KV-cache server metrics for the new concurrency-1 point.

**InferenceX** (`f2be63a98849591c40d80428cef5af6fb185417c`):

> Pin the GLM-5.2 B200 SGLang AgentX curve to lmsysorg/sglang:nightly-dev-cu13-20260901-07c8f729, which includes FlashInfer 0.6.18's BF16 TRTLLM MoE allocation fix.

> Use a 169 GB/rank HiCache target pool at c12 and c16 while retaining ratio mode for lower concurrencies.

> Isolate SGLang runtime caches per Slurm allocation to prevent concurrent sweep cells from sharing per-rank cache files.

**InferenceX** (`e807a5283c95dfa725df13c94d349196aea57820`):

> Refresh to collect TensorRT-LLM server metrics.

**InferenceX** (`3a7d6b714844a181a6a5ebdcfd4097ee04a0cf5e`):

> Switch the TP8 arm from EP=8 to EP=1: removing the MoE all-to-all collective reduces ITL p50 by ~5% at conc 4 (6.95 ms vs 7.3 ms baseline) and raises interactivity P90 from 105 to 110.5 tok/s/user. Extend the TP8 concurrency sweep to [4, 6, 8, 10] to cover the mid-range crossover region.

> Raise HiCache ratio from 1.5 to 2.5 and switch write policy to write_through_selective (per PR #2679): the agentic-coding corpus saturates any fixed DRAM pool at conc ≥ 10; the larger host tier sustains throughput through conc 12 (+12% tok/s/GPU vs conc 10). Trim the TP4 concurrency list to [1, 2, 4, 8, 10, 12] removing the untested conc 16 point.

> Double MAX_RUNNING_REQUESTS to 2×CONC: MTP draft+verify transiently batches more tokens than CONC sessions; the extra headroom prevents scheduler stalls under burst. Cap CUDA_GRAPH_MAX_BS at 64 (SGLang auto-interpolates a batch-size list up to the cap).

> Trim TP8/EP=1 concurrency list from [4, 6, 8, 10] to [4, 10]: sweep validation shows c6 and c8 dominated by the TP4/EP4 HiCache arm at those concurrency points.

## Stage 2 — deep dive

**Status: not yet run.**

Stage 2 is the `archwatch-deep-dive` skill: it names the *mechanism* this architecture introduces, assigns a bucket, and estimates how wrong BLIS is today.

| Bucket | Meaning |
| --- | --- |
| 0 | would not run — already decided above, deterministically |
| 1 | runs as-is; only fields BLIS already parses |
| 2 | known-gap mechanism; cite the `known-gaps.yaml` entry |
| 3 | new mechanism with no seam; name the functions needing a branch |

Everything above this marker is regenerated from the candidate on every run; the analysis is appended below it and is never overwritten.

<!-- archwatch:stage2:append-below -->
