---
schema: archwatch/3
arch_id: Kimi-K3
display_name: Kimi-K3
detected_at: '2026-09-04T20:04:51Z'
sources:
- inferencex
triggers:
- T5
- alias-join
markers:
- alias-join
join_edges:
- family:kimik3
significance:
- S3
bucket: null
bucket0_failures: []
silent_failures: []
silently_wrong: false
known_arch_drift: false
unparsed_fields: []
est_total_params: null
est_active_params: null
corroborated: false
model_types: []
model_ids: []
orgs: []
urls:
  inferencex.commit: https://github.com/SemiAnalysisAI/InferenceX/commit/cf08a54b17ef5fafcea1989d72383649a1e5c872
  inferencex.commit.2: https://github.com/SemiAnalysisAI/InferenceX/commit/c355f549ad06ae06675411fea1537ad589f41db7
  inferencex.commit.3: https://github.com/SemiAnalysisAI/InferenceX/commit/7ae9bf6edfd26dd8e843428113a2f7fad1b73bda
  inferencex.commit.4: https://github.com/SemiAnalysisAI/InferenceX/commit/9d751646e0715564c16515d3c4aecf6751c8ce3b
  inferencex.pr: https://github.com/SemiAnalysisAI/InferenceX/pull/2811
  inferencex.pr.2: https://github.com/SemiAnalysisAI/InferenceX/pull/2810
  inferencex.pr.3: https://github.com/SemiAnalysisAI/InferenceX/pull/2634
  inferencex.pr.4: https://github.com/SemiAnalysisAI/InferenceX/pull/2787
has_config: false
has_perf_notes: true
perf: []
stage2: pending
dry_run: true
---

# [archwatch] Kimi-K3 — new architecture detected

Stage-1 stub, generated by `archwatch` from deterministic checks only — no LLM ran, and nothing here was posted anywhere. Tracking only.

- **Verdict:** not checked — no `config.json` available yet
- **Architecture:** `Kimi-K3` (the pipeline's primary key)
- **Seen by:** InferenceX — single source

## Sources

| Source | Ref | Org | Evidence | Links |
| --- | --- | --- | --- | --- |
| InferenceX | `7ae9bf6edfd26dd8e843428113a2f7fad1b73bda` | — | InferenceX benchmarks Kimi-K3: perf-changelog entry in perf-changelog.yaml (keys: kimik3-fp4-gb200-dynamo-vllm-agentic, kimik3-fp4-b300-vllm-agentic-dspark) [commit 7ae9bf6ed] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/7ae9bf6edfd26dd8e843428113a2f7fad1b73bda) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2634) |
| InferenceX | `9d751646e0715564c16515d3c4aecf6751c8ce3b` | — | InferenceX benchmarks Kimi-K3: perf-changelog entry in perf-changelog.yaml (keys: kimik3-fp4-mi355x-vllm-agentic-mtp) [commit 9d751646e] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/9d751646e0715564c16515d3c4aecf6751c8ce3b) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2787) |
| InferenceX | `c355f549ad06ae06675411fea1537ad589f41db7` | — | InferenceX benchmarks Kimi-K3: perf-changelog entry in perf-changelog.yaml (keys: kimik3-fp4-mi355x-vllm-agentic-mtp) [commit c355f549a] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/c355f549ad06ae06675411fea1537ad589f41db7) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2810) |
| InferenceX | `cf08a54b17ef5fafcea1989d72383649a1e5c872` | — | InferenceX benchmarks Kimi-K3: perf-changelog entry in perf-changelog.yaml (keys: kimik3-fp4-gb300-dynamo-vllm-agentic-dspark-mooncake-dcp8-disagg, kimik3-fp4-gb300-dynamo-vllm-agentic-dspark-mooncake-dcp8-agg, kimik3-fp4-gb300-dynamo-vllm-agentic-mooncake-dcp8-agg) [commit cf08a54b1] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/cf08a54b17ef5fafcea1989d72383649a1e5c872) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2811) |

**How these signals were joined** — union-find over architecture id, normalized repo id and normalized family name. Every source row above was merged into this one report because of these edges:

- `family:kimik3` — normalized family name

> If any edge above is wrong, this stub has **fused two distinct architectures into one report** — worse than emitting two stubs, because the findings below then describe a model that does not exist. Check that every source row really is the same model before trusting the rest of this file.

## Why it fired

**Triggers** (any one is enough):

- **T5** — a curated benchmark / analyst entry names a model we had not seen

**Significance** (at least one must hold):

- **S3** — framework / analyst attention: a vLLM, SGLang or InferenceX signal

**Markers** (how this candidate was assembled, not why it matters):

- **`alias-join`** — no signal carried an `architectures[]` entry, so these signals were joined on a normalized display name — the `arch_id` above is that alias key, not a string read out of a config

## Deterministic findings (stage 1)

Computed by reading BLIS as text against the harvested support surface. No LLM, no simulation run — these are facts about the config, not a fidelity judgement.

### Bucket 0 — would BLIS accept this config?

**Not checked** — no `config.json` was obtainable for this architecture, so the validators could not run. Bucket is left undetermined.

### Config fields BLIS does not parse

_No config available, so no field diff could be computed._

### Parameter estimates

_Not estimated — the config lacked the fields the estimator needs._

## Third-party mechanism notes (verbatim)

Quoted from the source's own changelog or write-up — **their words, unverified, not archwatch's analysis.** Notes like these routinely name a mechanism days before a config is public, so read them as the earliest available hint about what stage 2 will have to model.

**InferenceX** (`cf08a54b17ef5fafcea1989d72383649a1e5c872`):

> Refresh the Kimi-K3 GB300 AgentX configurations with updated configs.

**InferenceX** (`c355f549ad06ae06675411fea1537ad589f41db7`):

> Bump the MI355X K3 vLLM recipe to vLLM ROCm nightly 7c5dc571 and LMCache 0.5.5rc3.

> Add a TP8/DCP8 spec-mtp DRAM-offload arm at concurrency 44/48/52.

> Retune the LMCache tier (12288-token chunk, 8 GPU workers) and the no-spec decode profile (gpu-memory-utilization 0.9, 8192 batched tokens); switch the DCP attention backend to ROCM_AITER_MLA.

**InferenceX** (`7ae9bf6edfd26dd8e843428113a2f7fad1b73bda`):

> Correct srt-slurm's external data-parallel process allocation so Kimi-K3 TP4/DP4 workers receive four GPUs per DP rank instead of one invalid rank per GPU.

> Configure model-specific tool and reasoning parsers for the Kimi-K3 and MiniMax-M3 serving paths so opt-in verifier requests produce structured OpenAI tool calls.

> Limit shared AgentX readiness and synthetic-acceptance rewrites to eval-only runs so default throughput recipe rendering remains unchanged.

**InferenceX** (`9d751646e0715564c16515d3c4aecf6751c8ce3b`):

> Bump the vLLM ROCm image from nightly-cb8104839c141609d99f1254459ef3a4f1bd4263 to nightly-1dc464d42681d22f38caf1fdc1eb632dc4421c45 and drop apply_k3_container_patches.sh (the patches are now baked into the image); add AITER_SITUV2_A8W4=1.

> Replace the vllm-simple DRAM offload arm with an LMCache MP-connector arm (lmcache 0.5.5.dev60+rocm7.2), running one per-node MP server with --chunk-size 3072 (the minimum multiple of the hybrid KDA/MLA 1536/3072 KV group sizes) and --separate-object-groups; mq_timeout raised to 6000s for the large agentic prefixes.

> Add decode context parallel (DCP_SIZE=8, --dcp-comm-backend a2a, TRITON_MLA attention backend) with GPU_MEM_UTIL lowered to 0.85, --max-num-batched-tokens 16384, and mla_prefill_backend=ROCM_AITER_FA.

> Make speculative decoding concurrency-adaptive: concurrency 1 uses 6 draft tokens (synthetic acceptance 3.75), concurrency <=10 uses 2 tokens (2.51), and higher concurrencies disable MTP; MAX_NUM_SEQS is now 2*CONC with CUDA-graph capture sizes scaled to (1+SPEC_NUM_TOKENS).

> Raise dram-utilization from 0.50 to 0.60, extend the none-offload conc-list from [1, 4, 8] to [1, 4, 8, 10, 12], and set the lmcache arm conc-list to [4, 8, 10, 12].

## Stage 2 — deep dive

**Status: not yet run.**

Stage 2 is the `archwatch-deep-dive` skill: it names the *mechanism* this architecture introduces, assigns a bucket, and estimates how wrong BLIS is today.

| Bucket | Meaning |
| --- | --- |
| 0 | would not run — already decided above, deterministically |
| 1 | runs as-is; only fields BLIS already parses |
| 2 | known-gap mechanism; cite the `known-gaps.yaml` entry |
| 3 | new mechanism with no seam; name the functions needing a branch |

Everything above this marker is regenerated from the candidate on every run; the analysis is appended below it and is never overwritten.

<!-- archwatch:stage2:append-below -->
