---
schema: archwatch/3
arch_id: DeepSeek-V4-Pro
display_name: DeepSeek-V4-Pro
detected_at: '2026-09-03T22:38:08Z'
sources:
- inferencex
triggers:
- T3
- T5
- alias-join
markers:
- alias-join
join_edges:
- family:deepseekv4pro
significance:
- S2
- S3
bucket: null
bucket0_failures: []
silent_failures: []
silently_wrong: false
known_arch_drift: false
unparsed_fields: []
est_total_params: null
est_active_params: null
corroborated: false
model_types: []
model_ids:
- deepseek-ai/DeepSeek-V4-Pro
orgs:
- deepseek-ai
urls:
  inferencex.commit: https://github.com/SemiAnalysisAI/InferenceX/commit/d13df3789276e7b34ab69abd506e082cd9a687de
  inferencex.commit.2: https://github.com/SemiAnalysisAI/InferenceX/commit/62c140aea726e35e134381bf123ffbe57143a95c
  inferencex.commit.3: https://github.com/SemiAnalysisAI/InferenceX/commit/5ecf6fdb01d3854c127356342e5c36b69d07b4de
  inferencex.commit.4: https://github.com/SemiAnalysisAI/InferenceX/commit/2bd473803b384947369b24ba8c553f1d3ffa55ad
  inferencex.commit.5: https://github.com/SemiAnalysisAI/InferenceX/commit/b5accc0893740e81efd68de415a777a719a31dac
  inferencex.commit.6: https://github.com/SemiAnalysisAI/InferenceX/commit/92debd9f12adaa680830963bf7be000cebc46280
  inferencex.commit.7: https://github.com/SemiAnalysisAI/InferenceX/commit/28303f72b2ed44545916b5c59f42eddec6749ce9
  inferencex.pr: https://github.com/SemiAnalysisAI/InferenceX/pull/2534
  inferencex.pr.2: https://github.com/SemiAnalysisAI/InferenceX/pull/2711
  inferencex.pr.3: https://github.com/SemiAnalysisAI/InferenceX/pull/2774
  inferencex.pr.4: https://github.com/SemiAnalysisAI/InferenceX/pull/2560
  inferencex.pr.5: https://github.com/SemiAnalysisAI/InferenceX/pull/2759
  inferencex.pr.6: https://github.com/SemiAnalysisAI/InferenceX/pull/2778
  inferencex.pr.7: https://github.com/SemiAnalysisAI/InferenceX/pull/2784
has_config: false
has_perf_notes: true
perf: []
stage2: pending
dry_run: true
---

# [archwatch] DeepSeek-V4-Pro — new architecture detected

Stage-1 stub, generated by `archwatch` from deterministic checks only — no LLM ran, and nothing here was posted anywhere. Tracking only.

- **Verdict:** not checked — no `config.json` available yet
- **Architecture:** `DeepSeek-V4-Pro` (the pipeline's primary key)
- **Org(s):** deepseek-ai
- **Seen by:** InferenceX — single source
- **Model repos:** `deepseek-ai/DeepSeek-V4-Pro`

## Sources

| Source | Ref | Org | Evidence | Links |
| --- | --- | --- | --- | --- |
| InferenceX | `28303f72b2ed44545916b5c59f42eddec6749ce9` | — | InferenceX benchmarks DeepSeek-V4-Pro: perf-changelog entry in perf-changelog.yaml (keys: dsv4-fp4-mi355x-sglang-agentic-mtp) [commit 28303f72b] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/28303f72b2ed44545916b5c59f42eddec6749ce9) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2784) |
| InferenceX | `2bd473803b384947369b24ba8c553f1d3ffa55ad` | deepseek-ai | InferenceX benchmarks DeepSeek-V4-Pro: benchmark config entry in configs/nvidia-master.yaml; perf-changelog entry in perf-changelog.yaml (keys: dsv4-fp4-b200-dynamo-sglang) [commit 2bd473803] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/2bd473803b384947369b24ba8c553f1d3ffa55ad) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2560) |
| InferenceX | `5ecf6fdb01d3854c127356342e5c36b69d07b4de` | — | InferenceX benchmarks DeepSeek-V4-Pro: perf-changelog entry in perf-changelog.yaml (keys: dsv4-fp4-gb300-dynamo-trt-agentx) [commit 5ecf6fdb0] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/5ecf6fdb01d3854c127356342e5c36b69d07b4de) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2774) |
| InferenceX | `62c140aea726e35e134381bf123ffbe57143a95c` | — | InferenceX benchmarks DeepSeek-V4-Pro: perf-changelog entry in perf-changelog.yaml (keys: dsv4-fp4-gb300-dynamo-trt-agentx) [commit 62c140aea] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/62c140aea726e35e134381bf123ffbe57143a95c) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2711) |
| InferenceX | `92debd9f12adaa680830963bf7be000cebc46280` | — | InferenceX benchmarks DeepSeek-V4-Pro: perf-changelog entry in perf-changelog.yaml (keys: dsv4-fp4-mi355x-atom-agentic-mtp) [commit 92debd9f1] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/92debd9f12adaa680830963bf7be000cebc46280) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2778) |
| InferenceX | `b5accc0893740e81efd68de415a777a719a31dac` | — | InferenceX benchmarks DeepSeek-V4-Pro: perf-changelog entry in perf-changelog.yaml (keys: dsv4-fp4-b300-sglang-agentic-hicache-mtp) [commit b5accc089] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/b5accc0893740e81efd68de415a777a719a31dac) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2759) |
| InferenceX | `d13df3789276e7b34ab69abd506e082cd9a687de` | — | InferenceX benchmarks DeepSeek-V4-Pro: perf-changelog entry in perf-changelog.yaml (keys: dsv4-fp4-b200-vllm) [commit d13df3789] | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/d13df3789276e7b34ab69abd506e082cd9a687de) [pr](https://github.com/SemiAnalysisAI/InferenceX/pull/2534) |

**How these signals were joined** — union-find over architecture id, normalized repo id and normalized family name. Every source row above was merged into this one report because of these edges:

- `family:deepseekv4pro` — normalized family name

> If any edge above is wrong, this stub has **fused two distinct architectures into one report** — worse than emitting two stubs, because the findings below then describe a model that does not exist. Check that every source row really is the same model before trusting the rest of this file.

## Why it fired

**Triggers** (any one is enough):

- **T3** — a frontier org published a new architecture
- **T5** — a curated benchmark / analyst entry names a model we had not seen

**Significance** (at least one must hold):

- **S2** — org track record: a frontier org, or an org whose top model clears the download threshold
- **S3** — framework / analyst attention: a vLLM, SGLang or InferenceX signal

**Markers** (how this candidate was assembled, not why it matters):

- **`alias-join`** — no signal carried an `architectures[]` entry, so these signals were joined on a normalized display name — the `arch_id` above is that alias key, not a string read out of a config

## Deterministic findings (stage 1)

Computed by reading BLIS as text against the harvested support surface. No LLM, no simulation run — these are facts about the config, not a fidelity judgement.

### Bucket 0 — would BLIS accept this config?

**Not checked** — no `config.json` was obtainable for this architecture, so the validators could not run. Bucket is left undetermined.

### Config fields BLIS does not parse

_No config available, so no field diff could be computed._

### Parameter estimates

_Not estimated — the config lacked the fields the estimator needs._

## Third-party mechanism notes (verbatim)

Quoted from the source's own changelog or write-up — **their words, unverified, not archwatch's analysis.** Notes like these routinely name a mechanism days before a config is public, so read them as the earliest available hint about what stage 2 will have to model.

**InferenceX** (`d13df3789276e7b34ab69abd506e082cd9a687de`):

> Refresh the DeepSeek-V4-Pro FP4 B200 vLLM image and search space, pin the Nscale NVFP4 checkpoint, use the FlashInfer CuTeDSL MoE backend for expert-parallel runs with EPLB disabled, set GPU memory utilization to 0.95, and cap the model length at 12288.

**InferenceX** (`62c140aea726e35e134381bf123ffbe57143a95c`):

> Set OMP_NUM_THREADS=1 for the prefill and decode workers across the GB300 Dynamo-TensorRT-LLM AgentX recipe matrix.

> Route AgentX replay and drain checks through the srt-slurm frontend endpoint when provided, with a localhost fallback.

**InferenceX** (`5ecf6fdb01d3854c127356342e5c36b69d07b4de`):

> Refresh to collect TensorRT-LLM server metrics.

**InferenceX** (`2bd473803b384947369b24ba8c553f1d3ffa55ad`):

> Add a DeepSeek-V4-Pro FP4 Dynamo-SGLang STP configuration for B200 with ten 8k/1k disaggregated topologies.

> Use Mooncake for KV transfer with raw synthetic benchmark inputs.

> Image: lmsysorg/sglang:nightly-dev-cu13-20260708-b3632494

> Switch the DeepSeek-V4-Pro FP4 B200 Dynamo-SGLang STP recipes to NIXL KV transfer.

> Refresh the SGLang image and Dynamo source revision, and align runtime environment flags with the updated image.

> Use raw synthetic benchmark inputs across the ten disaggregated 8k/1k STP recipes.

> Image: lmsysorg/sglang:nightly-dev-cu13-20260821-f825d729

> Enable the required DCGM power telemetry contract across all ten B200 disaggregated STP recipes.

**InferenceX** (`b5accc0893740e81efd68de415a777a719a31dac`):

> Update SGLang from lmsysorg/sglang-staging:dev-cu13-pr-35880 to lmsysorg/sglang:nightly-dev-cu13-20260827-20621aa1.

> Remove DEP8 HiCache concurrency 64 and 128: 128 ran out of GPU memory at mem-fraction-static=0.93; 64 was unmeasured and used the same memory budget.

**InferenceX** (`92debd9f12adaa680830963bf7be000cebc46280`):

> Split the DeepSeek-V4-Pro FP4 ATOM AgentX MTP sweep into two attention bands per the ROCm/ATOM DeepSeek-V4-Agentic-InferenceX.md recipe: tensor-parallel attention at concurrency 1, 2, 4, 8, and 16, and DP attention at concurrency 48, 64, 96, 128, and 256 (previously a single tensor-parallel band at concurrency 1, 2, 4, 8, 16, 32, and 48).

> The DP-attention band adds --enable-dp-attention and --enable-tbo plus ATOM's DPA routing knobs (ATOM_DP_SESSION_AFFINITY, ATOM_DP_LB_REQ_EQUIV, ATOM_ENABLE_PREFILL_DELAYER, ATOM_PREFILL_DECODE_INTERVAL), the two-batch-overlap prerequisites GPU_MAX_HW_QUEUES and ATOM_NUMA_BIND, and the AIPerf session-affinity vars that keep each agentic conversation pinned to one DP rank; the tensor-parallel band is otherwise unchanged.

> Session affinity is mandatory for the DP band: without it a session's turns scatter across DP ranks, the prefix KV written by one turn is unreachable by the next, and the multi-turn agentic workload collapses to cold prefill.

> The attention band is selected by the config's dp-attn, propagated to the recipe as DP_ATTENTION, so the script's former DP_ATTENTION=false hard-fail guard is removed.

> Bump the ATOM image from rocm/atom-dev:nightly_202608201032 to nightly_202608280858; FP8 KV/index caches, prefix caching, 32K state checkpoints, 16K batching/prefill chunks, FULL cudagraph mode, three-token MTP with golden AL 2.49, and max-num-seqs at twice concurrency are unchanged.

**InferenceX** (`28303f72b2ed44545916b5c59f42eddec6749ce9`):

> Bump the MI355X SGLang DeepSeek-V4-Pro FP4 AgentX MTP image from lmsysorg/sglang-rocm:v0.5.18-rocm720-mi35x-20260822 to lmsysorg/sglang-rocm:v0.5.18-rocm720-mi35x-20260829.

> Export TORCH_BLAS_PREFER_HIPBLASLT=1 to prefer hipBLASLt for dense GEMMs.

> Size chunked-prefill by tensor-parallel degree: 16384 tokens at TP8 and 8192 at TP4, erroring on any other TP.

> Lower GPU_MAX_HW_QUEUES from 5 to 2 on the DP-attention path.

> Add a TP8 EP1 no-offload spec-decoding mtp arm at concurrency 16.

## Stage 2 — deep dive

**Status: not yet run.**

Stage 2 is the `archwatch-deep-dive` skill: it names the *mechanism* this architecture introduces, assigns a bucket, and estimates how wrong BLIS is today.

| Bucket | Meaning |
| --- | --- |
| 0 | would not run — already decided above, deterministically |
| 1 | runs as-is; only fields BLIS already parses |
| 2 | known-gap mechanism; cite the `known-gaps.yaml` entry |
| 3 | new mechanism with no seam; name the functions needing a branch |

Everything above this marker is regenerated from the candidate on every run; the analysis is appended below it and is never overwritten.

<!-- archwatch:stage2:append-below -->
