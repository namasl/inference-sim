---
schema: archwatch/1
arch_id: WeirdActForCausalLM
display_name: WeirdAct 70B
detected_at: '2026-02-14T09:30:00Z'
sources:
- hf
triggers:
- T1
significance:
- S1
- S4
bucket: 0
bucket0_failures:
- hidden_act "gelu_pytorch_tanh" is not SwiGLU; rejected at sim/latency/kv_capacity.go:276
- torch_dtype "float4_e2m1" is unrecognized -> BytesPerParam=0; rejected at sim/latency/trained_physics_model.go:615
- num_key_value_heads is 0; must be > 0 (sim/latency/config.go:419-431)
unparsed_fields:
- ssm_state_size
- linear_attn_config
est_total_params: 71000000000
est_active_params: 71000000000
corroborated: false
model_types:
- weirdact
model_ids:
- labx/WeirdAct-70B
orgs:
- labx
urls:
  hf: https://huggingface.co/labx/WeirdAct-70B
has_config: true
perf: []
stage2: pending
dry_run: true
---

# [archwatch] WeirdActForCausalLM (WeirdAct 70B) — new architecture detected

Stage-1 stub, generated by `archwatch` from deterministic checks only — no LLM ran, and nothing here was posted anywhere. Tracking only.

- **Architecture:** `WeirdActForCausalLM` (the pipeline's primary key)
- **Model family:** WeirdAct 70B
- **`model_type`:** `weirdact`
- **Org(s):** labx
- **Seen by:** HuggingFace — single source
- **Model repos:** `labx/WeirdAct-70B`

## Sources

| Source | Ref | Org | Evidence | Links |
| --- | --- | --- | --- | --- |
| HuggingFace | `labx/WeirdAct-70B` | labx | Unrecognized torch_dtype and a non-SwiGLU activation | [hf](https://huggingface.co/labx/WeirdAct-70B) |

## Why it fired

**Triggers** (any one is enough):

- **T1** — new architecture whose config carries fields BLIS does not parse (unknown keys are silently dropped, so the risk is wrong numbers, not a crash)

**Significance** (at least one must hold):

- **S1** — scale: estimated total parameters at or above the threshold
- **S4** — popularity: downloads or likes over threshold, or seen on the trending sweep

## Deterministic findings (stage 1)

Computed by reading BLIS as text against the harvested support surface. No LLM, no simulation run — these are facts about the config, not a fidelity judgement.

### Bucket 0 — would BLIS accept this config?

**No — bucket 0.** 3 hard validator failures; BLIS would reject or mis-size this model before any simulation:

- hidden_act "gelu_pytorch_tanh" is not SwiGLU; rejected at sim/latency/kv_capacity.go:276
- torch_dtype "float4_e2m1" is unrecognized -> BytesPerParam=0; rejected at sim/latency/trained_physics_model.go:615
- num_key_value_heads is 0; must be > 0 (sim/latency/config.go:419-431)

### Config fields BLIS does not parse

2 architecture-relevant keys present in the config that BLIS never reads. BLIS drops unknown keys without warning, so each one is a candidate for a silently wrong number rather than an error:

- `ssm_state_size`
- `linear_attn_config`

### Parameter estimates

| Estimate | Approx. | Exact |
| --- | --- | --- |
| Total parameters | 71B | 71,000,000,000 |
| Active per token | 71B | 71,000,000,000 |

Estimated with standard transformer arithmetic from the config; approximate, and not a substitute for the published count.

## Config at a glance

Selected fields from the richest `config.json` seen (14 top-level keys in total).

| Field | Value |
| --- | --- |
| `model_type` | weirdact |
| `num_hidden_layers` | 48 |
| `hidden_size` | 6144 |
| `intermediate_size` | 16384 |
| `num_attention_heads` | 48 |
| `num_key_value_heads` | 0 |
| `vocab_size` | 128256 |
| `max_position_embeddings` | 131072 |
| `hidden_act` | gelu_pytorch_tanh |
| `torch_dtype` | float4_e2m1 |
| `tie_word_embeddings` | true |

## Stage 2 — deep dive

**Status: not yet run.**

Stage 2 is the `archwatch-deep-dive` skill: it names the *mechanism* this architecture introduces, assigns a bucket, and estimates how wrong BLIS is today.

| Bucket | Meaning |
| --- | --- |
| 0 | would not run — already decided above, deterministically |
| 1 | runs as-is; only fields BLIS already parses |
| 2 | known-gap mechanism; cite the `known-gaps.yaml` entry |
| 3 | new mechanism with no seam; name the functions needing a branch |

Bucket 0 is already established for this candidate — stage 2 only needs to say what the failing fields mean and how much it matters.

Everything above this marker is regenerated from the candidate on every run; the analysis is appended below it and is never overwritten.

<!-- archwatch:stage2:append-below -->
