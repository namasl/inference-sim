---
schema: archwatch/3
arch_id: Qwen3NextForCausalLM
display_name: Qwen3.8-Flash-Next
detected_at: '2026-02-14T09:30:00Z'
sources:
- hf
- inferencex
triggers:
- T1-known-arch
- T4
markers: []
join_edges:
- arch:qwen3nextforcausallm
- family:qwen3.8 flash next
significance:
- S2
- S3
bucket: null
bucket0_failures: []
silent_failures: []
silently_wrong: false
known_arch_drift: true
unparsed_fields:
- linear_conv_kernel_dim
- mamba_num_heads
- mamba_state_size
- mtp_num_layers
est_total_params: 402000000000
est_active_params: 17500000000
corroborated: true
model_types:
- qwen3_next
model_ids:
- qwen/Qwen3.8-Flash-Next
orgs:
- qwen
urls:
  commit: https://github.com/SemiAnalysisAI/InferenceX/commit/4d10ce7
  hf: https://huggingface.co/qwen/Qwen3.8-Flash-Next
has_config: true
has_perf_notes: true
perf:
- source: inferencex
  value:
  - hardware: 8xB200
    output_tok_per_s: 5120.0
    ttft_ms_p50: 190.0
    notes: sglang nightly, tp8
  - hardware: null
    output_tok_per_s: 6400.0
    notes: vendor claim, hardware unstated
  raw_ref: 4d10ce7
stage2: pending
dry_run: true
---

# [archwatch] Qwen3NextForCausalLM (Qwen3.8-Flash-Next) — known architecture, config grew fields BLIS does not parse

Stage-1 stub, generated by `archwatch` from deterministic checks only — no LLM ran, and nothing here was posted anywhere. Tracking only.

- **Verdict:** BLIS would run this config; no fatal or silent validator failure found (stage 2 still judges fidelity)
- **Architecture:** `Qwen3NextForCausalLM` (the pipeline's primary key)
- **Model family:** Qwen3.8-Flash-Next
- **`model_type`:** `qwen3_next`
- **Org(s):** qwen
- **Seen by:** HuggingFace, InferenceX — corroborated by 2+ sources
- **Model repos:** `qwen/Qwen3.8-Flash-Next`

## Sources

| Source | Ref | Org | Evidence | Links |
| --- | --- | --- | --- | --- |
| HuggingFace | `qwen/Qwen3.8-Flash-Next` | qwen | Known architecture string, but the config carries new Mamba and MTP keys | [hf](https://huggingface.co/qwen/Qwen3.8-Flash-Next) |
| InferenceX | `4d10ce7` | — | perf-changelog.yaml adds the model with a throughput row | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/4d10ce7) |

**How these signals were joined** — union-find over architecture id, normalized repo id and normalized family name. Every source row above was merged into this one report because of these edges:

- `arch:qwen3nextforcausallm` — architecture id
- `family:qwen3.8 flash next` — normalized family name

> If any edge above is wrong, this stub has **fused two distinct architectures into one report** — worse than emitting two stubs, because the findings below then describe a model that does not exist. Check that every source row really is the same model before trusting the rest of this file.

## Why it fired

> **This is not a new architecture.** `T1-known-arch` fired: `Qwen3NextForCausalLM` is already in BLIS's known set. What changed is the *config* — it now carries fields BLIS does not parse. Read this as drift in a family we thought we supported, not as a new model class.

**Triggers** (any one is enough):

- **T1-known-arch** — an architecture BLIS already knows, re-checked: its config has grown fields BLIS does not parse (config drift, not a new architecture)
- **T4** — corroboration: two or more independent sources named the same architecture

**Significance** (at least one must hold):

- **S2** — org track record: a frontier org, or an org whose top model clears the download threshold
- **S3** — framework / analyst attention: a vLLM, SGLang or InferenceX signal

## Deterministic findings (stage 1)

Computed by reading BLIS as text against the harvested support surface. No LLM, no simulation run — these are facts about the config, not a fidelity judgement.

### Bucket 0 — would BLIS accept this config?

**Yes — no fatal validator failed, and no silent failure was found either.** BLIS would load this config and produce numbers. Whether those numbers are *right* is still stage 2's call: unknown fields are dropped silently, so a clean pass here is not a clean bill of health.

### Config fields BLIS does not parse

4 architecture-relevant keys present in the config that BLIS never reads. BLIS drops unknown keys without warning, so each one is a candidate for a silently wrong number rather than an error:

- `linear_conv_kernel_dim`
- `mamba_num_heads`
- `mamba_state_size`
- `mtp_num_layers`

### Parameter estimates

| Estimate | Approx. | Exact |
| --- | --- | --- |
| Total parameters | 402B | 402,000,000,000 |
| Active per token | 17.5B | 17,500,000,000 |

Estimated with standard transformer arithmetic from the config; approximate, and not a substitute for the published count.

## Config at a glance

Selected fields from the richest `config.json` seen (23 top-level keys in total).

| Field | Value |
| --- | --- |
| `model_type` | qwen3_next |
| `num_hidden_layers` | 62 |
| `hidden_size` | 5120 |
| `intermediate_size` | 20480 |
| `num_attention_heads` | 40 |
| `num_key_value_heads` | 8 |
| `head_dim` | 128 |
| `vocab_size` | 151936 |
| `max_position_embeddings` | 1010000 |
| `hidden_act` | silu |
| `torch_dtype` | bfloat16 |
| `tie_word_embeddings` | false |
| `num_experts` | 512 |
| `num_experts_per_tok` | 10 |
| `moe_intermediate_size` | 1024 |

## Reported performance numbers

Carried in a signal's `extra["perf"]`. Recorded verbatim — these are future validation ground truth for BLIS, not archwatch's own measurements.

**InferenceX** (`4d10ce7`)

| `hardware` | `output_tok_per_s` | `ttft_ms_p50` | `notes` |
| --- | --- | --- | --- |
| 8xB200 | 5120.0 | 190.0 | sglang nightly, tp8 |
| — | 6400.0 | — | vendor claim, hardware unstated |

## Third-party mechanism notes (verbatim)

Quoted from the source's own changelog or write-up — **their words, unverified, not archwatch's analysis.** Notes like these routinely name a mechanism days before a config is public, so read them as the earliest available hint about what stage 2 will have to model.

**InferenceX** (`4d10ce7`):

> Qwen3.8-Flash-Next keeps the Mamba SSM state from the previous generation and adds a built-in MTP module, so the served token rate runs well ahead of what the layer count alone would suggest.
>
> Only the gated full-attention layers hold a KV cache; the SSM layers carry fixed-size state instead.

## Stage 2 — deep dive

**Status: not yet run.**

Stage 2 is the `archwatch-deep-dive` skill: it names the *mechanism* this architecture introduces, assigns a bucket, and estimates how wrong BLIS is today.

| Bucket | Meaning |
| --- | --- |
| 0 | would not run — already decided above, deterministically |
| 1 | runs as-is; only fields BLIS already parses |
| 2 | known-gap mechanism; cite the `known-gaps.yaml` entry |
| 3 | new mechanism with no seam; name the functions needing a branch |

Everything above this marker is regenerated from the candidate on every run; the analysis is appended below it and is never overwritten.

<!-- archwatch:stage2:append-below -->
