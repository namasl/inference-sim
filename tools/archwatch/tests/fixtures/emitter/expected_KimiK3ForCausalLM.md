---
schema: archwatch/2
arch_id: KimiK3ForCausalLM
display_name: Kimi K3
detected_at: '2026-02-14T09:30:00Z'
sources:
- hf
- inferencex
- vllm
triggers:
- T1
- T3
- T4
markers: []
significance:
- S1
- S2
- S3
bucket: null
bucket0_failures: []
silent_failures:
- MoE signalled but the expert count spelling "moe_num_experts" is not in ResolveNumExperts's alias set -> the 896 experts are never resolved and a 1T sparse model is sized and simulated as dense, behind one logrus.Warnf (sim/latency/kv_capacity.go:552)
- KDA / linear-attention layers are charged as full attention in step time and weights; no config field tells BLIS otherwise (sim/latency/trained_physics_model.go:297)
silently_wrong: true
known_arch_drift: false
unparsed_fields:
- moe_num_experts
- n_group
- topk_group
- mtp_num_layers
- mtp_loss_weight
- router_gate_type
- attention_sink_tokens
est_total_params: 1026000000000
est_active_params: 38400000000
corroborated: true
model_types:
- kimi_k3
model_ids:
- moonshotai/Kimi-K3-Instruct
- moonshotai/Kimi-K3-Base
orgs:
- moonshotai
urls:
  commit: https://github.com/SemiAnalysisAI/InferenceX/commit/9f2c1ab
  config: https://huggingface.co/moonshotai/Kimi-K3-Instruct/blob/main/config.json
  hf: https://huggingface.co/moonshotai/Kimi-K3-Instruct
  pr: https://github.com/vllm-project/vllm/pull/21877
has_config: true
has_perf_notes: true
perf:
- source: inferencex
  value:
  - hardware: 8xH200
    output_tok_per_s: 1840.0
    ttft_ms_p50: 412.0
    cost_per_mtok_usd: 0.61
    notes: vLLM 0.11.1, tp8, 1k/1k in/out
  - hardware: null
    output_tok_per_s: 2310.0
    notes: vendor-claimed, hardware unstated
  raw_ref: 9f2c1ab
stage2: pending
dry_run: true
---

# [archwatch] KimiK3ForCausalLM (Kimi K3) — new architecture detected

Stage-1 stub, generated by `archwatch` from deterministic checks only — no LLM ran, and nothing here was posted anywhere. Tracking only.

- **Verdict:** BLIS **runs this and reports wrong numbers** — 2 silent failures, no error raised
- **Architecture:** `KimiK3ForCausalLM` (the pipeline's primary key)
- **Model family:** Kimi K3
- **`model_type`:** `kimi_k3`
- **Org(s):** moonshotai
- **Seen by:** HuggingFace, InferenceX, vLLM — corroborated by 2+ sources
- **Model repos:** `moonshotai/Kimi-K3-Instruct`, `moonshotai/Kimi-K3-Base`

## Sources

| Source | Ref | Org | Evidence | Links |
| --- | --- | --- | --- | --- |
| HuggingFace | `moonshotai/Kimi-K3-Instruct` | moonshotai | New repo from a frontier org; config carries 5 fields BLIS does not parse | [config](https://huggingface.co/moonshotai/Kimi-K3-Instruct/blob/main/config.json) [hf](https://huggingface.co/moonshotai/Kimi-K3-Instruct) |
| InferenceX | `9f2c1ab` | — | MODELS.md diff adds the model with a first perf row | [commit](https://github.com/SemiAnalysisAI/InferenceX/commit/9f2c1ab) |
| vLLM | `vllm#21877` | — | PR title names the architecture \| adds vllm/model_executor/models/kimi_k3.py | [pr](https://github.com/vllm-project/vllm/pull/21877) |

## Why it fired

**Triggers** (any one is enough):

- **T1** — new architecture whose config carries fields BLIS does not parse (unknown keys are silently dropped, so the risk is wrong numbers, not a crash)
- **T3** — a frontier org published a new architecture
- **T4** — corroboration: two or more independent sources named the same architecture

**Significance** (at least one must hold):

- **S1** — scale: estimated total parameters at or above the threshold
- **S2** — org track record: a frontier org, or an org whose top model clears the download threshold
- **S3** — framework / analyst attention: a vLLM, SGLang or InferenceX signal

## Silently wrong today — BLIS runs this and reports confident nonsense

**2 silent validator failures.** BLIS does not crash, does not refuse the config and does not raise an error. It logs a warning at most, then reports numbers that are wrong. This is the failure class archwatch exists to catch: a wrong answer nobody is told about.

- MoE signalled but the expert count spelling "moe_num_experts" is not in ResolveNumExperts's alias set -> the 896 experts are never resolved and a 1T sparse model is sized and simulated as dense, behind one logrus.Warnf (sim/latency/kv_capacity.go:552)
- KDA / linear-attention layers are charged as full attention in step time and weights; no config field tells BLIS otherwise (sim/latency/trained_physics_model.go:297)

> **The bucket-0 verdict below is clean, and that is exactly what makes this dangerous.** The config sails through validation, so nothing downstream ever signals a problem — BLIS will happily produce numbers, and they will be wrong. Treat any BLIS output for this architecture as unusable until stage 2 says otherwise.

## Deterministic findings (stage 1)

Computed by reading BLIS as text against the harvested support surface. No LLM, no simulation run — these are facts about the config, not a fidelity judgement.

### Bucket 0 — would BLIS accept this config?

**Yes, BLIS would run it — and this is the dangerous case, not the safe one.** No *fatal* validator failed, but 2 **silent** failures were found above: BLIS accepts the config, runs, and reports wrong numbers with no error to show for it. A clean bucket 0 here means nothing will ever tell you.

### Config fields BLIS does not parse

7 architecture-relevant keys present in the config that BLIS never reads. BLIS drops unknown keys without warning, so each one is a candidate for a silently wrong number rather than an error:

- `moe_num_experts`
- `n_group`
- `topk_group`
- `mtp_num_layers`
- `mtp_loss_weight`
- `router_gate_type`
- `attention_sink_tokens`

### Parameter estimates

| Estimate | Approx. | Exact |
| --- | --- | --- |
| Total parameters | 1.03T | 1,026,000,000,000 |
| Active per token | 38.4B | 38,400,000,000 |

Estimated with standard transformer arithmetic from the config; approximate, and not a substitute for the published count.

## Config at a glance

Selected fields from the richest `config.json` seen (30 top-level keys in total).

| Field | Value |
| --- | --- |
| `model_type` | kimi_k3 |
| `num_hidden_layers` | 61 |
| `hidden_size` | 7168 |
| `intermediate_size` | 18432 |
| `num_attention_heads` | 64 |
| `num_key_value_heads` | 64 |
| `vocab_size` | 163840 |
| `max_position_embeddings` | 262144 |
| `hidden_act` | silu |
| `torch_dtype` | bfloat16 |
| `tie_word_embeddings` | false |
| `num_experts_per_tok` | 8 |
| `moe_intermediate_size` | 2048 |
| `n_shared_experts` | 1 |
| `first_k_dense_replace` | 1 |
| `kv_lora_rank` | 512 |
| `q_lora_rank` | 1536 |
| `qk_rope_head_dim` | 64 |
| `qk_nope_head_dim` | 128 |
| `v_head_dim` | 128 |
| `rope_theta` | 50000.0 |

## Reported performance numbers

Carried in a signal's `extra["perf"]`. Recorded verbatim — these are future validation ground truth for BLIS, not archwatch's own measurements.

**InferenceX** (`9f2c1ab`)

| `hardware` | `output_tok_per_s` | `ttft_ms_p50` | `cost_per_mtok_usd` | `notes` |
| --- | --- | --- | --- | --- |
| 8xH200 | 1840.0 | 412.0 | 0.61 | vLLM 0.11.1, tp8, 1k/1k in/out |
| — | 2310.0 | — | — | vendor-claimed, hardware unstated |

## Third-party mechanism notes (verbatim)

Quoted from the source's own changelog or write-up — **their words, unverified, not archwatch's analysis.** Notes like these routinely name a mechanism days before a config is public, so read them as the earliest available hint about what stage 2 will have to model.

**InferenceX** (`9f2c1ab`):

> Kimi K3 moves to 896 routed experts with 8 active, and replaces most full-attention layers with KDA layers that hold no KV cache at all.
> Only every fourth layer keeps a conventional KV cache, which is where the memory-per-token win comes from.

## Stage 2 — deep dive

**Status: not yet run.**

Stage 2 is the `archwatch-deep-dive` skill: it names the *mechanism* this architecture introduces, assigns a bucket, and estimates how wrong BLIS is today.

| Bucket | Meaning |
| --- | --- |
| 0 | would not run — already decided above, deterministically |
| 1 | runs as-is; only fields BLIS already parses |
| 2 | known-gap mechanism; cite the `known-gaps.yaml` entry |
| 3 | new mechanism with no seam; name the functions needing a branch |

Everything above this marker is regenerated from the candidate on every run; the analysis is appended below it and is never overwritten.

<!-- archwatch:stage2:append-below -->
